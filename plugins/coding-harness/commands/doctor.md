---
description: Verify canonical document structure, Rules declarations and administrator checklist.
---

In the current project directory, run `node "${CODEBUDDY_PLUGIN_ROOT}/bin/harness.mjs" doctor`. Report missing canonical files, forbidden legacy roots, Rules without `适用场景:`, incomplete checklist items and Git collaboration-contract problems. Return the runtime's single next action; do not infer readiness from file presence alone.
