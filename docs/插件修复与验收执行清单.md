# Coding Harness 0.7.0 修复与验收清单

更新时间：2026-09-10。工作目录：`D:/1_Work/Tencent-work资料/ai-coding-harness`。

## 1. 现行口径

- [x] 《技术设计-流程模块与交接协议.md》与 `ADR-latest.md` 的 0.7.0 决策增补是现行合同；旧版方案、字段、路径和模板仅作历史记录，不得加载或生成。
- [x] 用户入口以顶层自然语言 Skill 为主；命令仅是确定性兜底。
- [x] 初始化由一位管理员串行发起：全量扫描 → 路径骨架 → 主 Agent 按实际依赖分发 subagent → 各负责人直接回写真实内容 → 汇总与 Rules 等待上游 → checklist 确认 → Git 提交。没有中间人工审核回合。
- [x] Git 是唯一共享版本与历史依据；不引入 digest、内容哈希、CAS、跨进程初始化锁或第二套确认链。
- [x] 业务仓库只允许 `docs/knowledge/`、`docs/function/`、`docs/workflows/` 与 `.codebuddy/` 规范根；不得生成 `gates.md`、`harness-self.md`、`核心约束.md`、`启动入口.md` 或未登记 Markdown。

## 2. 插件目录逐项核查

| 目录 | 现状与职责 | 是否复制到业务仓库 |
| --- | --- | --- |
| `.codebuddy-plugin/` | Plugin manifest，版本 0.7.0 | 否 |
| `agents/` | `code-reviewer.md` 单一源 | 复制到 `.codebuddy/agents/` |
| `bin/`、`commands/` | 自然语言备用 CLI 与排障入口 | 否 |
| `hooks/` | SessionStart、PreToolUse；先判定 Harness 作用域 | 否 |
| `runtime/` | 初始化、全量扫描、Markdown 合同、知识、导航、状态、门禁、doctor | 否 |
| `schemas/`、`scripts/` | 状态 Schema、包与模板校验 | 否 |
| `skills/` | 顶层编排与阶段 Skills | 由 CodeBuddy Plugin 加载 |
| `templates/project/` | 静态入口与目标布局来源；prepare 只落静态入口/骨架 | 按初始化阶段生成 |
| `templates/business/`、`templates/engineering/` | 业务/工程 Markdown 合同 | 按真实识别结果直接生成 |
| `templates/workflow/` | README + 九份正文，共十份 workflow 产物 | 创建任务时生成 |
| `tests/` | 结构、运行时、Hooks、模板和安全回归 | 否 |

## 3. 已完成修复

- [x] `discoverProject` / `scanProjectMaterials` 不预设原始材料位置，枚举所有可见文件和目录，仅排除 `.git`、`node_modules`（workflow 运行态在生成导航时排除）。
- [x] `phase=prepare` 只写静态入口、共享配置、reviewer、空索引和路径骨架；不落带占位符的项目总览、工程/业务知识、Rules 或架构图。`phase=finalize` 在完整草案和八项 checklist 全部确认后写入事实；缺少确认时在任何长期资产写入前停止。
- [x] 文件树探索态只有路径行；完成态覆盖每个可见文件和目录并要求真实用途，支持目录键带 `/` 的草案回写。
- [x] 0.7 结构化知识草案包含项目用途/技术栈/软件设计架构、工程模块九段、功能字段、五份 Rules 和证据；校验安全名称、路径覆盖、证据路径、架构图和旧 `{name,summary}` 形状拒绝。
- [x] Markdown allowlist 解析精确校验标题层级/顺序、表头/分隔符、逐文件声明占位符、完成态无占位、路径/证据覆盖与 Rules 知识入口；黑名单正则仅为补充诊断。十份 workflow 模板也有结构解析回归。
- [x] 业务功能主动生成 `module.json`、`function.json`、功能描述与无历史伪造的功能演变历史；工程模块直接生成九段事实文档。
- [x] 项目总览加入紧随技术栈的“软件设计架构”；组件图同时生成 `component.puml` 与面向人的 `component.svg`。
- [x] Rules 五文件精确集合，首行 `适用场景:`，固定四段且只做约束与知识索引，不复制知识正文。
- [x] `doctor` 检查根目录 allowlist、全文档合同、链接可达、文件树全覆盖、SVG/PUML、Git 忽略与 Git 跟踪；只允许已登记结构。
- [x] Hooks 先判定有效 `.codebuddy/harness.json`：未接入项目完全 no-op，损坏接入 fail-closed，已接入项目保留状态/共享契约/preCommit 保护；状态路径字面量、Shell/环境变量、复合命令、重定向、复制/移动/解释器写入均拒绝。
- [x] apply 前置检查知识资产冲突；不覆盖已有项目文档、索引或 Rules。

## 4. 验证记录

- [x] `npm test --prefix plugins/coding-harness`：64/64 通过。
- [x] `npm run validate --prefix plugins/coding-harness`：包目录、manifest、Rules、模板合同、workflow 集合和 Hook matcher 通过。
- [x] 临时项目烟测：真实目录/文件全量树 → prepare 骨架 → finalize 事实文档 → SVG → 八项 checklist → Git 跟踪 → 新材料刷新树 → workflow 停在 `source_materials`；未接入 Hook no-op。
- [x] 四类独立 subagent 验收完成：Rules/checklist、模板 allowlist、流程编排均已复核；发现的 checklist 门禁、Rules 结构/入口、模板动态事实、workflow 依赖与 Hook 绕过均已修复并回归。

## 5. 独立验收（交付前关闭）

- [x] Rules：五文件、具体适用场景、可达 knowledge/function/source 入口、操作装配与职责边界。
- [x] Checklist/配置：八项自然语言引导、草案→确认→应用、配置字段、Git 忽略/跟踪检查；确认无 digest/CAS/额外锁。
- [x] 流程编排：动态 subagent 依赖图、唯一读取负责人、直接回写、失败重派、需求阶段逐次止损、命令备用路径、Hook 作用域及 Shell fail-closed。
- [x] 模板鲁棒性：每个模板路径、标题层级、章节、表头/分隔符、逐文件占位符 allowlist、完成态解析与技术设计逐字 workflow 合同。

## 6. 发布与 Git

- [x] 构建 `dist/ai-market-0.7.0.zip`（111 个条目），检查 ZIP 条目与源文件逐项字节一致，入口 README、Marketplace manifest、Plugin manifest 和 package 版本均为 0.7.0。
- [x] 在本资料目录初始化 Git，只提交本版源码、设计文档、验收记录、发布包和构建脚本；不提交无关演示素材。
- [x] 提交消息使用 `release: coding harness 0.7.0`；协作只依赖 Git 提交，不新增摘要/哈希协议。

## 7. 明确未声称的范围

- 未在真实 CodeBuddy Desktop/CLI 宿主完成安装后的端到端联调；只验证插件包、Node 运行时、Hook 输入输出和临时 Git 项目。
- 外部工单、代码平台、CI、日志/监控 MCP 仍按项目适配器接入，不由本版伪造默认配置。
- subagent 任务图仅存在当前初始化会话，不向业务仓库新增任务图 JSON 或锁文件。
