# ADR · AI 赋能开发全流程冷启动体系

> **现行一致性口径（2026-09-14）**：生成项目的目录、Rules、十一份 workflow 产物、`state.json` 字段、模板字段契约和知识审核枚举，均以《技术设计-流程模块与交接协议》为唯一规范。本 ADR 中与其不一致的历史方案、路径、规则文件名、阶段名、字段或版本号全部失效；ADR 仅保留不冲突的决策理由、权限隔离、原子写入、证据与门禁原则。当前插件版本为 `0.8.1`，`harness.json` 适配器合同仍为 `0.8.0`。

> 本文档记录"做哪些机制"与"为什么这样取舍"的决策过程，逐阶段推进。  
> 每条决策按四段结构展开：开发流程 / 机制确认 / 具体实现手段 / 取舍。

**决策依据**

- 《企业级 AI Agent 及 Coding 实践方法论》（Harness Engineering 工程体系，含逐字稿注解与应用宝活动平台内部实证）
- 资料库《AI Coding 规约体系 · 交互总览》
- 《AI 辅助开发带来的研发流程变化》六阶段流程图（CSIG 技术委员会）

**内部 KM / 乐享实践参考（持续补充）**

> 以下材料用于校验机制是否有内部实践基础，不将其中的效率数据直接作为本体系承诺；具体机制仍以本 ADR 的逐项人工确认结果为准。

| 参考材料 | 与本体系相关的已验证实践 | 目前可吸收的结论 |
| --- | --- | --- |
| [药箱 TAPD 需求单知识库构建](https://csig.lexiangla.com/pages/b23f19232ef44ea7aae976f3f824c14e) | 将全量 TAPD 历史需求按“业务模块 → 核心功能”组织；每个功能点维护`功能描述.md`与`功能演变历史.md`，并通过`module.json`、`function.json`维护映射；后续只处理新增或变化需求。 | 功能知识库应以模块—功能点为最小可查询单元，当前功能认知与演变历史分离，索引只做定位；这为设计前的业务定位与按需加载提供基础。 |
| [6A 工作流：Align → Architect → Atomize → Approve（CloudReporter 案例）](https://csig.lexiangla.com/pages/ee1223c5e0674aa08f2ccd546fd8100c) | 在架构设计前先提出配置、异常策略等关键决策问题；再形成架构图、组件/接口定义、数据流与异常策略；随后拆为带输入/输出契约及依赖关系的原子任务，并在编码前审批。 | “探索/澄清 → 设计 → 任务拆分 → 人工确认”是可复用的设计链路；设计前应先暴露并确认真正影响方案的决策点。 |
| [TDev 的关键设计](https://csig.lexiangla.com/pages/bd6eaa221cdb407388fd83e85fdec46d) | Explore-before-Plan；Coverage Matrix 双向追踪；Scope of Impact 前置分析，并用结构化文件保存探索结论供后续阶段消费。 | 不应从需求直接跳任务或代码；先探索现有工程和影响范围，再规划；需求、设计项、任务之间需可追溯。 |
| [AI 赋能研发全流程提效：Knot Skill 工具链实践总结](https://csig.lexiangla.com/pages/1d83233b276e485b89690c9579f2442c) | TAPD 驱动实现设计、API 文档、DB 设计，后续代码与验证 Skill 消费上游产物；DB 设计会与真实表结构和实体类交叉验证。 | 设计产物应分层依赖、逐层给下游供上下文；接口和数据设计不能只凭需求猜测，必要时须回到真实工程事实核验。 |
| [设计文档是分阶段构造的上下文](https://csig.lexiangla.com/pages/f620fd0f0dea43cea05c16aa8869f6ee) | 将概要设计、详细设计、任务拆分视为逐层压缩和装配上下文的过程，避免 AI 一口气从需求生成大量代码。 | 设计文档的价值不是“写文档”，而是为每个后续任务准备刚好够用、信噪比高的上下文。 |
| [任务粒度控制](https://csig.lexiangla.com/pages/1e4abcb714924189b4f335d0132b6f4a) | 单一 Plan 聚焦单一功能模块；过大的 Plan 会导致命名、接口风格和状态管理不一致；复杂需求应按技术依赖自然拆分。 | 任务粒度不能只按文件数或时长切分，应以“一个可验证的功能/工程责任单元”为边界，并保持同一 Plan 的上下文聚焦。 |
| [关键步骤 2：Tasks（内部原子任务实践）](https://csig.lexiangla.com/pages/077901aec8c449dd91b7223c07b1125c) | 将原子任务定义为目标明确、可独立执行和验证的单元；文中提出“10–15 分钟最小可执行任务”的特定实践口径。 | 10–15 分钟可作为某类自动化终端动作的参考，不能机械作为本体系统一阈值；本体系以独立验收、上下文可控和依赖清晰作为主判据。 |

**交付形态**（已决策）

第一版采用 **CodeBuddy Plugin 核心 + 项目仓库适配层**。Plugin 作为产品与统一分发载体，在内部插件市场集中维护通用 Skills、独立评审 SubAgent、CodeBuddy Hooks 和确定性执行器；业务仓库保存本项目的安装声明、构建/测试/扫描配置、Rules、流程文档与运行状态。项目已有的 Git Hook 与 CI 可按 `harness.json` 声明的门禁命令自行接入，不是初始化器固定生成的目录。当前只设计 CodeBuddy 版本，不承担 Claude Code 或 Codex 的兼容。

项目首次接入存在一个明确的引导安装步骤：项目负责人先从内部插件市场安装 Harness Plugin，再在业务仓库内使用自然语言“为当前项目接入 AI Coding Harness”或备用入口 `/coding-harness:init` 启动初始化。主 Agent 枚举全部可见项目文件和目录，运行时写入路径骨架；不把 README、`docs/`、固定目录名或扩展名当作原始材料位置。随后依据实际树动态分发互斥的工程与业务路径给对应 subagent：业务 Agent 只写 `docs/function/**` 和业务入口，工程 Agent 只写项目总览、工程模块和组件图源；Rules Agent 最后只写 Rules。文件树和 SVG 由运行时一次性写入，所有 subagent 均不得回写树项。每个可读路径只分配一个阅读负责人，失败由主 Agent 重派；subagent 回报只含路径、未识别项和必要交接，CLI 只显示计数与识别提示。顶层 Skill 以自然语言逐项引导负责人完成八项 checklist；全部事实完整且无占位符才允许应用。管理员将共享契约提交到 Git，其他成员只拉取。

自然语言“开始一轮开发”不授权模型一口气完成 workflow：它创建 workflow 时先全量枚举项目可见文件并登记为原始材料索引，再只追加用户本轮原话、链接或外部材料位置，状态停在 `source_materials`。下一次明确调用才允许候选评审；再下一次负责人确认候选后才允许发布正式需求；进入设计仍等待后续调用。`state.json` 的 `step`、`summary` 和 `next_action` 是唯一流程游标，每次调用只执行一个游标动作，不能以一段模糊功能描述推断为已经授权需求、设计和开发。

后续成员克隆仓库并启动 CodeBuddy 时，`.codebuddy/settings.json` 声明内部市场和启用的 Plugin，使 CodeBuddy 自动安装/启用同一能力。初始化是单个管理员串行完成的一次性动作；其他成员拉取已确认配置，不重复初始化。

以下文件属于团队共享的**仓库契约**，必须纳入 Git 管理：

```text
CODEBUDDY.md                    # 常驻层上下文，≤200 行
.codebuddy/settings.json        # Plugin 市场、安装与启用声明
.codebuddy/harness.json         # 本项目构建、测试、扫描和门禁配置
.codebuddy/rules/               # 按需层规约，每份声明适用场景
.codebuddy/onboarding-checklist.json # 管理员接入确认记录
.codebuddy/agents/               # 项目只读评审与三类初始化 subagent 定义
```

AI 和初始化器不得把上述路径加入 `.gitignore`、`.git/info/exclude` 或其他忽略规则，也不得以“本地 AI 配置”为由建议忽略它们。初始化自检应验证这些仓库契约未被忽略；管理员提交后还应验证其已被 Git 跟踪。只有 `.codebuddy/workflows/` 等机器运行态目录和个人本地覆盖配置可以被忽略。

**范围口径**（已决策）

*范围全链、深度分档。六阶段全部占位（状态位、门禁位、回退位在状态文件中定义完整），  
但只有不依赖外部平台的环节实装，依赖平台的环节留适配器 + ONBOARDING 声明。*

---

## 〇、全链机制清单与判定

判定分三档：

| 档位     | 含义                                            |
| ------ | --------------------------------------------- |
| **实装** | plugin 装上即可跑，不依赖团队的外部平台                       |
| **留位** | 状态位与命令占好，实现依赖外部平台或外部输入，给适配器契约 + ONBOARDING 说明 |
| **不做** | 本期明确不做，写清触发条件（出现什么信号才做）                       |

### 0.1 图中六阶段节点判定

| #  | 阶段 | 节点         | 图中归属   | 判定      | 说明                    |
| -- | -- | ---------- | ------ | ------- | --------------------- |
| 1  | 需求 | 需求提出       | 人工     | 实装      | 录入原始诉求，初始化状态文件        |
| 2  | 需求 | 需求扩写       | AI     | 实装      | 补边界、异常分支、非功能要求        |
| 3  | 需求 | 历史需求比对     | AI     | 实装      | 关键词 + 目录索引检索，不建向量库    |
| 4  | 需求 | 需求文档生成     | AI     | 实装      | 含需求级测试用例章节            |
| 5  | 需求 | 文档确认       | 人工     | 实装      | **硬闸口一**，不通过回到需求理解    |
| 6  | 设计 | 研发架构设计     | 人工     | 实装（人主导） | AI 只出候选与影响面           |
| 7  | 设计 | 前端设计       | AI     | 实装      |                       |
| 8  | 设计 | API 设计     | AI     | 实装      | 接口契约，是后续验证的断言基准       |
| 9  | 设计 | 架构变更分析     | AI     | 实装      | 判定风险档位的输入             |
| 10 | 设计 | 前端视觉生成（沙箱） | AI     | **留位**  | 依赖设计稿输入与预览环境          |
| 11 | 设计 | 研发任务分解     | AI + 人确认开工 | 实装 | AI 生成任务包；人只确认范围、契约、验收与开工条件 |
| 12 | 开发 | 装配任务上下文 | AI | 实装 | 按任务包定位目录、文件与设计结论，不要求人手工粘贴 |
| 13 | 开发 | 单元测试先行 | AI | 实装 | TDD：先按任务验收标准写或更新失败测试 |
| 14 | 开发 | 代码编写 | AI | 实装 | 最小实现使测试通过，随后重构 |
| 15 | 开发 | 自动验收门禁 | AI + 脚本 | 实装 | 集成后 lint、编译/类型、任务测试、全量测试、构建全部通过 |
| 16 | 开发 | 生成版本摘要与验收结果 | AI | 实装 | 由 Diff、任务验收和门禁结果生成，供人复核并作为 MR 描述基础 |
| 17 | 开发 | 研发任务验收与提交 MR | 人确认 + AI | 实装 | **硬闸口二**：AI 展示验收结果，人复核确认；确认后 AI 提交 MR，转入独立 AI 代码评审 |
| 18 | 评审 | 代码智能评审     | AI     | 实装      | 必须由独立 Agent 执行        |
| 19 | 评审 | 安全问题扫描     | 安全工具 + AI 语义复核 | 实装 | 优先调用项目已有扫描器，AI 补充业务语义安全检查 |
| 20 | 评审 | 代码与需求映射保存  | AI     | 实装      | 可追溯性的落点               |
| 21 | 评审 | 生成合并报告与知识更新审核单 | AI     | 实装 | 合并依据与长期资产审核分文件交付 |
| 22 | 评审 | MR 审核与批准 | 人工 | 沿用平台能力 | 不新增 AI 审批能力；使用代码平台原生审核流程 |
| 23 | 发布 | 交付内容整理 | — | **不做** | MR 描述与代码合并报告已经覆盖，不再生成重复材料 |
| 24 | 发布 | 变更影响范围总结 | — | **不做** | 已并入代码合并报告，不在合并后重复生成 |
| 25 | 发布 | 运维发布 | 现有 CI/CD | **体系外承接** | 合并事件按项目原有配置自动触发流水线，Harness 不接管发布 |
| 26 | 运维 | 日志异常分析     | 图中仍为人工 | **留位**  | 必接日志平台 MCP            |
| 27 | 运维 | BUG 分析     | 图中仍为人工 | **留位**  | 依赖日志与监控数据             |

> 观察：原图运维阶段两个节点在"AI 赋能后"一行仍标为人工参与色，说明该阶段尚未 AI 化。  
> 而 PDF 逐字稿里这一段恰恰是收益最高的——把 CLS 日志、SkyWalking 做成 MCP 挂上，  
> 定位 Bug 效率"10 倍、20 倍都不止"。这是图与方法论之间的一处缺口，本体系按留位处理，  
> 接上 MCP 后即可转实装。

### 0.2 图中没有但必须补的横切机制

这些机制不属于任何单一阶段，但缺任何一条，上面 27 个节点在三个月后都会失效。

| # | 横切机制          | 判定 | 缺了会怎样                        |
| - | ------------- | -- | ---------------------------- |
| A | 状态文件（流程状态落盘）  | 实装 | 对话历史被压缩后进度丢失，跨会话无法接续，阶段间无法串联 |
| B | 质量门禁（提交前物理阻断） | 实装 | AI 代码"表面可用、内部藏风险"，缺陷带绿色标记进生产 |
| C | 经验账本与升格（越用越好） | 实装 | 上周踩的坑这周还踩，整套体系只是一次性脚手架       |
| D | 用户引导层（统一入口与导航） | 实装 | 成员需要记命令、理解内部状态机才能用，接入成本压过收益 |
| E | 上下文装载（三层：常驻/按需/强制） | 实装 | 前面所有产物都写好了，但没有机制保证 AI 真的读到，渐进披露沦为一条期望 |

判断 C 有没有建起来，只看一个问题：**上周踩的坑，这周 AI 还会不会再踩一次？**

判断 E 有没有建起来，只看一个问题：**把 `业务入口.md` 改坏，AI 的行为会不会变？** 不会变，说明它从来没被真正装载过。

### 0.3 本期明确不做

| 机制                                  | 不做的理由               | 什么信号出现才做            |
| ----------------------------------- | ------------------- | ------------------- |
| 五层知识库 / 三维正交 / 三级成熟度 + 自动衰减         | 冷启动只建项目层，"空壳比缺失更危险" | 知识条目超过 50 条         |
| Worktree 并行与 Fork-Join              | 增加冲突治理复杂度，冷启动无收益    | 单需求任务数过 6 且有并行空间    |
| 门禁风险分档矩阵（minimal/low/high/critical） | 起步不分档，全量跑           | 门禁跑太久成为瓶颈           |
| 升格频率统计脚本                            | 账本为空时统计不出任何频率       | 经验条目超过 30 条         |
| 代码驱动编排（强类型主程序调度）                    | 渐进式确定性优于一步到位确定性     | 混合编排跑通并收集到高频失败模式    |
| DORA 指标 / ACMM 分级 / OTel 埋点         | 是给人做判断的框架，进上下文即噪声   | 单独做成人读自评页，不进 plugin |
| Loop Engineering 四类循环               | 业界仍处早期，token 成本方差极大 | 开发闭环稳定运行后另立议题       |
| 按需层向量检索                             | 显式声明适用场景比语义相似度更可控、可审查、可自检 | 按需层文档量增长到明显匹配瓶颈    |
| 三层统一加载调度器                           | 三层机制各自成熟且互不依赖，叠一层自维护抽象无收益 | 混合方式跑通并收集到高频失效模式  |

> 上下文布局遵循《技术设计-流程模块与交接协议》第 2 节：不建立额外约束目录或路径注入清单。约束写入五份 Rules，可确定判断的检查接入 Hook / CI。

### 0.4 产物存储总布局（唯一口径）

本节按《技术设计-流程模块与交接协议》第 2 节记录业务仓库生成布局。插件源码、安装教程和实现计划属于插件仓库，不复制进业务仓库。

```text
<project-root>/
├── .gitignore                       # 保留既有规则，只追加机器运行态忽略项
├── CODEBUDDY.md                    # 常驻层上下文，≤200 行
├── docs/
│   ├── knowledge/                  # 长期工程知识
│   │   ├── 项目总览.md
│   │   ├── 文件树.md
│   │   ├── 业务入口.md
│   │   ├── architecture/           # .puml 源文件 + 渲染 svg
│   │   └── modules/                # 工程模块说明，按需创建
│   ├── function/                   # 长期业务功能知识
│   │   ├── module.json             # 业务模块索引
│   │   └── <业务模块>/
│   │       ├── function.json       # 功能点索引
│   │       └── <功能点>/
│   │           ├── 功能描述.md
│   │           └── 功能演变历史.md
│   └── workflows/                  # 从原始诉求到合并的完整流程实例
│       ├── README.md               # 任务总索引
│       └── <task-id>/
│           ├── README.md
│           ├── source-materials.md # 原始材料索引：链接、文件路径、来源说明
│           ├── candidate-review.md # 候选需求、历史对齐、人工审核
│           ├── requirement.md      # 审核发布后的正式需求单
│           ├── design-alignment.md
│           ├── design-decision.md
│           ├── development-contract.md
│           ├── task-package.md
│           ├── development-summary.md
│           ├── knowledge-update-review.md # 长期知识与 Rules 的独立审核交付物
│           └── merge-report.md
└── .codebuddy/
    ├── settings.json               # Plugin 市场、安装与启用声明
    ├── harness.json                # 构建、测试、扫描与门禁配置
    ├── rules/                      # 按需层规约
    ├── onboarding-checklist.json   # 管理员接入确认记录
    ├── agents/
    │   ├── code-reviewer.md        # 从插件 agents 源定义复制的只读评审 Agent
    │   ├── business-knowledge-writer.md # 初始化业务知识写入 Agent
    │   ├── engineering-knowledge-writer.md # 初始化工程知识写入 Agent
    │   └── rules-writer.md         # 初始化 Rules 写入 Agent
    └── workflows/                  # 机器运行态，默认 gitignore
        └── <task-id>/
            ├── state.json
            └── evidence/
```

**三条布局规则**

1. **按读者分区**：`docs/` 保存人读文档及其轻量 JSON 索引、架构图源文件和渲染图；`.codebuddy/workflows/` 是机器运行态。`.codebuddy/` 其余内容是团队共享配置，必须由 Git 跟踪，不得整体忽略。
2. **按时效分区**：`docs/knowledge/` 与 `docs/function/` 是长期知识，验收后仍然有效；`docs/workflows/<task-id>/` 是本轮流程实例，验收后保留为任务记录但不进入后续默认上下文。
3. **workflow 是完整事务边界**：从原始材料索引、候选评审、正式需求，到设计、开发、评审、知识更新审核和合并报告，全部只落在同一 `docs/workflows/<task-id>/` 下；不得为候选稿或需求模板另建跨工作流目录。
4. **`docs/knowledge/` 与 `docs/function/` 平级且职责互斥**：前者回答“这个工程怎么组织、代码在哪、架构为什么这样”，并保存跨模块稳定的 API、数据与集成语义；后者回答“系统有哪些业务功能、规则是什么、为什么演变成现在这样”。功能点属业务知识，不放进 `knowledge/`。

**文件命名口径**

| 区域 | 命名 | 理由 |
| --- | --- | --- |
| `docs/knowledge/`、`docs/function/` | 中文文件名 | 主要供人浏览，中文更直观 |
| `docs/workflows/<task-id>/` | 英文固定文件名 | 被 `state.json` 的 `artifacts` 字段、CI 与脚本引用，需规避编码与跨平台风险 |
| `.codebuddy/` 全部 | 英文文件名 | 机器读取与配置解析 |

因此第二章中的中文产物名只是**概念名**，落盘一律使用英文名：

| 概念名 | 落盘文件 |
| --- | --- |
| 设计对齐稿 | `docs/workflows/<task-id>/design-alignment.md` |
| 设计结论 | `docs/workflows/<task-id>/design-decision.md` |
| 共同开发契约 | `docs/workflows/<task-id>/development-contract.md` |
| 任务包 | `docs/workflows/<task-id>/task-package.md` |
| 正式需求单 | `docs/workflows/<task-id>/requirement.md` |
| 版本摘要与验收结果 | `docs/workflows/<task-id>/development-summary.md` |
| 长期知识更新审核单 | `docs/workflows/<task-id>/knowledge-update-review.md` |
| 代码合并报告 | `docs/workflows/<task-id>/merge-report.md` |

**术语消歧**：`docs/knowledge/modules/` 指**工程模块**（代码组织单元）；`docs/function/<业务模块>/` 指**业务模块**（业务能力划分）。两者不是同一概念，也不要求一一对应。

**共享开发知识库**：`docs/knowledge/api/`、`data/`、`integration/` 是第三类长期工程知识，面向跨模块、跨任务复用的共享开发事实，而非某轮设计档案。目录固定如下：

```text
docs/knowledge/
├── api/
│   ├── README.md                 # API 总索引、权威 OpenAPI/IDL 位置、版本与兼容原则
│   └── <领域或服务>.md            # 接口清单、请求响应、错误语义、兼容要求与事实依据
├── data/
│   ├── README.md                 # 数据总索引、权威 DDL/Migration 位置与变更原则
│   ├── <数据域>.md                # 实体、表关系、字段语义、索引、约束与事实依据
│   └── erd.puml / erd.svg        # 可版本管理的 ER 图源及默认供人阅读的渲染图
└── integration/
    ├── README.md                 # 外部系统、RPC、消息事件总索引
    └── <系统或事件域>.md          # 提供/消费方、Schema、认证、幂等、顺序与失败处理
```

OpenAPI/IDL、Migration/DDL、受控 Schema、Topic/网关/适配器配置才是可执行事实的权威来源；Markdown 负责导航、语义解释、关系与兼容边界，并必须链接事实依据。workflow 的 `development-contract.md` 只记录本轮增量、负责方、使用方和验证方式，不能替代长期库。验收后由 `knowledge-update-review.md` 决定是否回写：已验收的 API、数据或集成契约变化必须更新对应长期条目；纯本轮临时 Mock、试验性实现或未确认方案不得写入。

---

## 一、需求阶段

### 1.1 开发流程

**在链路中的位置**：全链起点，负责把未结构化的业务材料转成经人确认、可进入设计阶段的正式需求；同时为项目建立和持续更新“系统现在是什么、为什么会这样”的功能知识。

**承上**：没有上游工程阶段。输入可以是 TAPD、旧 PRD、版本记录、会议纪要、聊天记录、客户邮件、截图或口头描述。它们都是待整理材料，不天然等于正式需求。

**启下**：输出两类按时间接力的资产：

1. **正式需求单**：本轮迭代的动作工单，描述“这次准备改变什么、做到什么算完成”。它只服务于尚未验收的迭代：设计、开发和验收都直接读取它；可发布到 TAPD、Jira、飞书项目等正式系统，或在无外部系统时按来源批次发布为本地 Markdown。
2. **功能知识库**：描述验收后“系统现在是什么、为什么会这样”。每轮迭代验收后，正式需求单中的有效变更统一收敛到对应业务模块与功能点；它不替代本轮迭代的需求工单。

**阶段边界**：本阶段不决定技术方案，不写接口字段、不定代码实现。正式需求只写业务主张和业务级验收标准；技术设计与字段级断言由设计阶段补齐。

**需求线内的顺序**：

```text
来源材料
→ 按原意整理、拆分为初始候选需求
→ 定位候选需求涉及的业务模块 / 功能点
→ 历史对齐：按需读取功能描述与功能演变历史
→ 输出“可能已存在 / 可能冲突 / 可能复用”提示
→ 优先补齐非功能需求；仅在用户故事定义不清时谨慎生成【AI扩写】候选
→ 在本轮 workflow 写入原始材料索引与候选评审稿
→ 人工审核
→ 逐条获得发布资格
→ 人显式发布为正式需求
```

**历史对齐参与时点**：它位于“初始候选需求形成”之后、“候选评审稿交给人审核”之前。先有当前需求的明确主张，AI 才能准确定位应查哪个功能；又必须在人工审核前完成，历史冲突、重复和可复用提示才能成为人裁定边界的依据。历史对齐不改写原始材料，也不自动把历史规则变成新需求；它只提供带依据的提示。

**AI 扩写边界**：AI 优先补齐非功能需求中的缺口，即质量/技术约束（性能、安全、可用性、兼容性等）和业务规则（条件、异常、权限、计算、合规等）。对用户故事保持克制：原材料已经清楚表达角色、能力和业务价值时，只按原意整理；仅当角色、能力边界或业务价值本身定义不清，且已有功能知识能提供明确依据时，才谨慎提出独立的`【AI扩写】`用户故事候选。

### 1.2 机制确认

#### 机制一：AI 整理、条件性扩写；人统一审核

AI 先按原意整理和拆分来源材料，明确材料不得被改写成“AI 版本”。扩写优先针对非功能需求的缺口：质量/技术约束和业务规则可在材料不清晰、缺失或未定义时，基于已有功能知识提出补充。用户故事只在角色、能力边界或业务价值本身定义不清时谨慎扩展；不得因为历史中存在相似功能，就凭空新增用户目标或业务价值。

候选需求只使用以下三种语法：

| 类型 | 语法 | 适用内容 |
| --- | --- | --- |
| 用户故事（默认） | 作为 `[角色]`，我希望 `[能力]`，以便 `[业务价值]`。 | 新功能、流程优化、用户可感知能力 |
| 业务规则 | 当 `[条件]` 时，系统必须 `[行为]`，以确保 `[业务结果]`。 | 权限、审批、异常、计算、合规规则 |
| 质量/技术约束 | 在 `[场景]` 下，系统应满足 `[可验证要求]`。 | 性能、安全、可用性、兼容性、技术债 |

AI 补充出的候选需求在标题或主张前标注 `【AI扩写】`；普通候选需求不加标记。该标记只存在于候选评审稿，正式发布后移除。

候选需求写入本轮 `docs/workflows/<task-id>/candidate-review.md`，同目录的 `source-materials.md` 只记录原始材料链接、路径和来源说明，不复制原文。每条候选需求使用固定审核结论：`待审核` / `通过` / `需修改` / `合并至 R-xx` / `拆分为 R-xx` / `驳回` / `暂缓`，并以自然语言填写审核意见。

人的角色是业务裁定者：决定需求是否成立、边界是否正确、是否合并或拆分。AI 可按明确审核意见返工，但任何 AI 新生成或改写内容都必须回到`待审核`，不得继承“通过”。当前评审稿只展示有效候选条目；被合并、拆分或驳回的旧条目不保留为墓碑，完整历史由 Git 负责。

#### 机制二：正式需求单就是本轮迭代动作工单

审核通过后，人显式发布正式需求单；它直接进入本轮迭代，供设计、开发与验收读取，不再额外维护一套“需求状态资产”。

- 已接 TAPD、Jira、飞书项目等系统时：由对应适配器创建迭代工作项。
- 未接外部系统时：在本轮流程实例目录下发布 `docs/workflows/<task-id>/requirement.md`；它就是本轮迭代工单，不是草稿。

正式需求单只保留需求编号、类型/需求主张和业务级验收标准；不保留来源证据、候选审核过程或`【AI扩写】`标识。验收前，它是唯一需要被读取的需求工单；验收后，不再作为后续需求的默认输入。

#### 机制三：迭代验收后，需求工单收敛为功能知识

正式需求单回答“本轮要做什么”；功能知识库回答“当前系统是什么、为什么会这样”。两者在时间上接力，而不是长期并行堆积：**验收前读正式需求单，验收后读功能知识库。**

功能知识库按“**业务模块 → 功能点**”组织。每个功能点只保留两份有读取价值的业务知识：

| 文件 | 职责 | 默认读取时机 |
| --- | --- | --- |
| `功能描述.md` | 当前有效的功能、规则、边界和主要流程 | 新需求定位到该功能后优先读取 |
| `功能演变历史.md` | 每轮已验收迭代收敛后的关键变更、当时问题与必要取舍，保留功能“为什么” | 需要解释历史原因、判断兼容或冲突时按需读取 |

独立代码评审通过后，AI 根据正式需求、设计、Diff 与验证结果先生成 `knowledge-update-review.md` 中的功能知识更新草案；经负责人审核通过后，才直接覆盖`功能描述.md`中被替代的旧内容，并将本轮真正有必要保留的变化写入`功能演变历史.md`。更新完成后，`requirement.md` 随本轮任务目录保留为任务记录，但**不再进入后续任务的默认上下文**——它从“当前工单”降级为“历史记录”，读取入口切换到 `docs/function/`。不为当前读取额外保留工单副本或状态记录。

一条已落地需求若确实影响多个已有功能点，允许由 AI 自动在这些功能点各自的唯一`功能演变历史.md`中写入面向该功能的记录。这是必要的业务视角重复，不要求人维护多份文本，也不额外建立一套原始需求归档层。

AI 不得根据代码或常识编造业务动机；原材料未说明“为什么”时必须明确标注“未说明/待补充”。

#### 机制四：历史对齐是提示，不替人下结论

新需求分析时，先定位所属业务模块和功能点，再按需读取相关`功能描述.md`；只有发现可能重复、冲突、需要解释历史边界时，才读取相关`功能演变历史.md`。

输出只可表述为：`可能已存在` / `可能冲突` / `可能复用`，并说明关联功能和依据；最终由人裁定。功能知识库未初始化时，需求分析仍可继续，但必须明确提示“功能知识库未初始化，已跳过历史对齐”，不得伪造“系统没有历史功能/需求”的结论。

### 1.3 具体实现手段

#### 两套 Skill 的职责边界

| Skill | 触发场景 | 读取/写入对象 | 产出 |
| --- | --- | --- | --- |
| 历史功能知识初始化 Skill（Skill A） | 存量项目冷启动，或补录历史材料 | 旧 TAPD 导出、PRD、版本记录、会议纪要等；写功能知识库 | 模块/功能点目录、功能描述草案、功能演变历史 |
| 需求分析工作流 Skill（Skill B） | 日常输入新需求材料 | 本轮 workflow 的原始材料索引、候选评审稿、正式需求；按需读取功能知识库 | `source-materials.md`、`candidate-review.md`、历史对齐提示、`requirement.md` |

两个 Skill 仍保持职责独立，并支持用户以自然语言发起；它们由统一的 CodeBuddy Plugin 分发。Plugin 同时提供 `/coding-harness:init` 作为项目首次接入和故障排查所需的确定性备用入口，但日常使用不要求程序员记忆命令。独立代码评审由 Plugin 内的专用 SubAgent 承担；Plugin 只承载通用能力，项目差异继续由仓库中的 `.codebuddy/harness.json` 等适配文件声明。

#### 项目资产布局

完整路径定义见 0.4 节。本阶段只涉及两处：

```text
docs/workflows/<task-id>/         # 原始材料索引、候选评审、正式需求均在同一流程实例
docs/function/                    # 功能知识库：业务模块 → 功能点
```

`source-materials.md`、`candidate-review.md` 与 `requirement.md` 是同一 workflow 的连续状态，不再建立 `docs/requirements/`、`candidates/`、`templates/` 或 `published/` 目录。第一版使用 Harness 内置模板；同一份需求或候选稿两处落盘必然产生同步问题。

功能知识库统一置于 `docs/function/`，与 `docs/knowledge/`（工程知识）平级且职责互斥：功能点是业务知识，回答“系统有哪些业务能力、规则如何演变”；`docs/knowledge/` 回答“工程如何组织、代码在哪、架构为什么这样”。全文出现「功能知识库」时，均指 `docs/function/`。

`module.json`和`function.json`仅承担轻量、可重建的定位索引：稳定 ID、名称、别名、目录路径，以及需求与模块/功能点的映射。业务事实只写 Markdown，不在索引中复制正文，也不维护多余状态字段。分类采用“已有模块/功能优先匹配；新增分类由人确认后创建目录”的受控机制，避免近义目录膨胀。

#### 自动化与人工确认边界

- Skill A 自动提取与分类历史材料，生成或更新功能知识草案；低置信度分类和新增模块/功能点必须提示人工确认。
- Skill B 自动整理、按需扩写、生成候选评审稿和历史对齐提示；不得直接发布外部工作项。
- 人在 Markdown 中完成候选审核；人显式确认发布目标后，Skill B 才执行发布。
- 独立代码评审通过后，AI 以正式需求、设计、Diff 与验证证据生成独立的长期知识更新审核单；人审核通过后，才直接覆盖`功能描述.md`中的旧内容，并将必要变更收敛进`功能演变历史.md`；随后 `requirement.md` 降级为任务记录，不再进入后续默认上下文，任务目录本身保留。

### 1.4 取舍

#### 取舍一：AI 是否对每条材料都扩写？

不做逐条扩写。只对不清晰、缺失、未定义的部分提出`【AI扩写】`候选需求。

原因：明确的 TAPD 或业务材料已经是业务决策，AI 重写只会引入噪声和伪造感。保留条件性扩写，是为了让缺失边界、异常路径或未定义规则有机会进入人工审核，而不是被沉默地遗漏。

#### 取舍二：候选需求是否直接写 TAPD？

不直接写。先进入按来源批次的本地 Markdown 评审稿，人工审核并显式发布后才写外部系统。

原因：候选整理和正式工作项是两种不同状态；前者允许 AI 提议、合并和返工，后者必须稳定、可追责。该设计也让没有 TAPD 等系统的客户可以完整运行流程。

#### 取舍三：功能知识库是否取代需求库？

不取代。功能知识记录已确认、已落地的系统认知；需求记录当前或未来的变更承诺、审核与发布。

原因：把两者混在一起，会让“准备做”被误读为“系统已经有”，也会让功能现状被需求流转状态污染。它们通过“新需求读取功能知识；已实施需求回流功能知识”形成闭环。

#### 取舍四：跨功能需求要不要避免重复？

允许必要重复。一个功能点只维护一份自己的功能历史；一条变更确实影响多个功能时，可自动进入多个相关功能点的历史。

原因：从每个功能视角查历史时，都必须看得到影响自己的关键变更。要避免的是人工手工复制和多处不一致，而不是消灭业务视角上的重复。

#### 取舍五：是否引入外置知识库或向量检索？

第一版不以外置知识库为前提，也不引入向量检索。采用项目内模块—功能点目录和 Markdown 按需加载。

原因：目标客户可能有存量系统但没有系统化历史需求管理；冷启动阶段，结构化业务目录比全量语义检索更可控。代价是跨语义的关联可能漏检，接受由人工审核兜底；知识量和检索需求增长到明显瓶颈后，再单独讨论检索升级。

#### 取舍六：功能“为什么”是否默认塞进每次 AI 上下文？

不默认全量加载。普通分析优先读取`功能描述.md`；只在历史对齐、冲突分析、需要解释规则来源时按需读取`功能演变历史.md`。

原因：保留“为什么”对业务连续性至关重要，但把完整历史塞进每次上下文会引入噪声、增加成本，也会让 AI 混淆已废弃和当前有效规则。

#### 取舍七：是否给知识条目维护复杂状态、失效日期和多层归档？

不做。正式需求单只在本轮迭代验收前作为工单读取；验收后降级为任务记录、不再进入默认上下文，变更收敛为功能知识。功能描述直接覆盖旧内容；不再有用的知识文件直接删除，版本追溯统一交给 Git。

这里区分“降级”与“删除”：`docs/workflows/<task-id>/` 是本轮任务的完整记录，按 7.6 保留不自动清理；被降级的只是它在上下文装载中的地位，而不是文件本身。真正会被删除的是失效的长期知识文件。

原因：本体系的目标是让 AI 和团队快速读到当前有效信息，而不是建设一套知识资产管理系统。额外状态、有效期、归档层会增加维护动作与检索噪声；Git 已足以承担恢复、对比和审计责任。

---

## 二、设计阶段

### 2.1 开发流程

**在链路中的位置**：承接尚未验收的正式需求单，完成工程探索、关键设计判断与任务拆分；输出已可执行的任务包后，才算设计完成。

**承上**：读取本轮正式需求单，并按需读取对应的功能知识；源码仍是实现细节的事实来源。

**启下**：输出面向本轮实施的设计与可执行任务，供开发、测试和集成消费；本轮任务完成并验收后，需求工单不再继续占用默认上下文。

### 2.2 机制确认

#### 机制一：以 Claude Code `/init` 的最终效果为起点，先建立可用的项目导航底座

项目知识初始化不生成脱离代码的 Wiki；它先生成完整文件树，再按树逐个读取实际文件、补全一句话用途，最后一次写完除 workflow 外的全部规范长期资产。真实缺口由负责人在 checklist 中纠正，不允许以占位骨架冒充完成。第一轮初始化至少必须产出以下三项：

| 产物 | 作用 | 内容边界 |
| --- | --- | --- |
| `docs/knowledge/项目总览.md` | 达到 Claude Code `/init` 类似的快速入门效果 | 项目用途、技术栈、软件设计架构、启动/构建/测试入口、顶层模块、未识别项和事实依据；保持简短，不展开实现细节 |
| `docs/knowledge/文件树.md` | 让人和 AI 都能先看见完整项目结构 | 先列出所有可见的项目文件和目录路径骨架，再由负责人回写每项基于阅读的一句话用途。`.git`等系统目录不列；不只列“关键文件” |
| `docs/knowledge/业务入口.md` | 让 AI 按业务问题进入正确的知识和代码范围 | 按业务模块列出“遇到什么问题 / 先读取哪些功能知识、工程说明和源码路径”，而非要求 AI 每次从全仓猜入口 |

`文件树.md`是自动生成并随文件结构变化刷新的导航索引，不要求人手工维护；初始化时先写路径骨架，分区负责人完成阅读后回写真实用途。它与`业务入口.md`只负责定位目录和文件路径，不复述代码逻辑；代码始终是行为和实现细节的第一事实。

上述三份产物统一存放于 `docs/knowledge/` 根下（见 0.4 节），全部归属**按需层**，不得进入常驻层：`文件树.md`要求完整列出项目可见文件，中型仓库即达数百至上千行，进常驻层会直接耗尽上下文预算。常驻层只保留指向它们的索引条目和触发条件（详见第十一章）。`业务入口.md`承担按需层的业务侧索引职责，但它本身也位于按需层，因此必须由常驻层显式指向，否则 AI 不会读取一个它不知道存在的文件。

#### 机制二：架构以 UML 作为系统级展现，不用大段文字或代码复述

架构说明采用可版本管理的 PlantUML 源文件（`.puml`）保存于 `docs/knowledge/architecture/`，并在同目录渲染为 SVG 供人阅读；`.puml` 源与渲染产物均纳入 Git，以保证不装 PlantUML 的成员也能直接看图。它说明系统边界、组件关系、关键业务流和不可破坏的架构取舍；不抄录函数、字段或算法实现。

第一轮初始化默认生成一张**组件图**，展示前端、服务、任务、数据存储、消息系统和外部依赖的边界与关系。后续按真实问题增补：

- 改变跨模块业务流程时，生成对应的**时序图**；
- 改变状态生命周期时，生成对应的**状态图**；
- 只有涉及部署、网络或运行环境变更时，生成**部署图**；
- 不默认生成类图或完整调用图，代码本身才是这类细节的第一事实。

#### 机制三：初始化由依赖图分发，负责人确认 checklist，不增加中间审核回合

初始化由主 Agent 维护会话内依赖图：主 Agent 扫描路径，运行时先写路径骨架；工程和业务 subagent 分别直接写互不重叠的工程/功能文档，工程路径完成后业务 Agent 写业务入口，Rules subagent 最后写五份规则及知识入口。`文件树.md` 与组件 SVG 始终由运行时统一写入，subagent 不得写树或 SVG。主 Agent 只负责调度、失败重派和确定性结构校验，不把所有源码集中到一个上下文；subagent 回报只含必要路径和交接，也不设置额外的人审中间回合。负责人只需在顶层 Skill 的自然语言 checklist 中确认或纠正事实，Git 是共享版本依据。

#### 机制四：工程知识采用混合式渐进披露，源码仍是实现事实

工程知识不建设覆盖全仓的代码说明库，也不为每条知识添加大量状态、基线或生命周期字段。它按以下路径逐步披露：

```text
项目总览.md
→ 业务入口.md
→ 架构 UML
→ 按需创建的工程模块说明（`docs/knowledge/modules/`）
→ 定位到相关目录与文件
→ 实际代码、接口定义和配置核验
```

目录和文件路径是导航接口，代码、接口定义和配置文件才负责回答“现在具体怎么实现”。长期工程知识只保留能显著减少探索成本的内容：跨服务/跨端/跨仓关系、架构取舍、兼容边界、不可违反的工程约束、非代码配置、生效机制与已确认的易误判点。

这条披露路径本身只是**读取顺序**，不是装载机制。它能否真正生效，取决于第十一章定义的三层上下文装载：常驻层必须列出这条路径上每一环的位置与触发条件，按需层每份文档必须声明适用场景，否则本机制退化为对 AI 自觉性的期望。

内容仍然有效就不动；每轮迭代确实改变其事实时，直接修改为当前正确表述，旧表述直接删除。无价值或与源码重复的文档直接删除；版本追溯由 Git 负责。

#### 机制五：AI 先生成设计对齐稿；人只确认真正需要业务或技术裁定的事项

每轮正式需求单确定后，AI 根据业务入口、目录定位、架构 UML 与实际代码生成**设计对齐稿**（落盘 `docs/workflows/<task-id>/design-alignment.md`）。它是人与 AI 的设计协作界面，不是最终设计文档，固定包含五段：

```text
1. 需求理解：本轮改变什么、不改变什么、与验收标准的对应关系
2. 改动范围：涉及的业务模块、目录/文件路径、必须保持的现有规则
3. 架构变化：本轮涉及的 UML 图；不涉及架构变化时明确写“不涉及”
4. 待人确认的决策：AI 推荐方案、可选方案、差异与影响，以及需要人确认的问题
5. 风险与不确定项：代码和需求均不能确认的内容，以及需要补充的事实
```

人不需要审核或逐项确认 AI 已通过代码核验的工程事实；人只显式确认会影响业务目标、兼容性、成本、交付风险或不可逆操作的待决策项，并可修正改动范围和 AI 的理解偏差。AI 根据确认意见返工，直到没有未决决策。

#### 机制六：对齐完成后，依次输出设计结论、共同开发契约与任务包

设计对齐稿中的讨论完成后，AI 输出三份面向执行的产物（落盘路径见 0.4 节）：

| 产物 | 只保留什么 | 不保留什么 |
| --- | --- | --- |
| 设计结论（`design-decision.md`） | 已确认的改动范围、边界、最终 UML、关键取舍，以及需求验收标准的实现覆盖 | 备选方案、讨论过程、已否决内容和契约正文 |
| 共同开发契约（`development-contract.md`） | 全部开发任务共同读取的 API、公共接口、数据、跨任务集成和必要共享行为 | 设计理由、普通验收行为和不适用的契约块 |
| 任务包（`task-package.md`） | 后续开发、测试与集成可直接执行的任务及其契约 ID 引用 | 任务拆分前的探索过程和契约正文 |

任务包采用“共同开发契约先定、独立责任并行、集成验证收口”的模型，不用传统`前置任务 / 后置任务`字段制造无效等待。每个任务固定说明：

```text
任务目标：要独立交付的工程结果
负责契约：本任务负责实现的共同开发契约 ID
使用契约：本任务依赖的共同开发契约 ID
可改范围：允许修改的目录、文件或模块边界
验收标准：完成后必须可观察、可验证的结果
联调条件：何时、以什么输入输出与其他任务集成
硬阻塞：确实无法隔离、必须等待的前提；没有则明确“无”
```

验收标准必须从正式需求单的业务级验收标准拆解而来，并落到任务可直接验证的表达；不得只写“完成开发”“自测通过”等不可证伪表述。任务的粒度定义为：**一个 AI 或开发者能在一轮聚焦上下文中完成、验证并交付的最小工程责任。**不以固定时长、固定文件数或“前端/后端/测试”工种划分为硬标准。一个合格任务必须同时满足：单一结果、上下文可控、可改范围明确、共享契约已定、验收可独立进行，并且拆开后存在真实并行价值。若任务上下文明显膨胀、验收无法独立、或与其他任务频繁争抢相同核心文件和契约，才继续拆分。

共同开发契约独立落在 `development-contract.md`，作为所有开发任务共同读取的唯一契约正文。它按需装配 HTTP API、公共接口、数据、跨任务集成和共享行为五类块；普通状态、权限和行为结果属于验收标准，只有多个任务必须共享的状态、权限、事务或并发语义才提升为共享行为契约。`task-package.md` 只引用契约 ID，不复制正文。契约已明确、可用 Mock/Stub 替代真实实现的，不构成等待；各任务可直接并行。硬阻塞只记录真正无法隔离的前提。

#### 机制七：人确认任务包可开工，设计阶段才结束

AI 生成任务包后，向人汇总四项开工判断：

```text
1. 任务是否完整覆盖正式需求单的验收标准？
2. 共同约定是否足以支持任务并行，是否仍有未定关键规则？
3. 是否存在真实硬阻塞；如有，应先解决什么？
4. 任务边界是否存在明显遗漏、重复或错误拆分？
```

人只可选择：`确认开工` / `要求调整任务包` / `暂不进入开发`。这不是技术细节 Review，而是确认团队即将投入的工作正确、完整且具备执行条件。只有人`确认开工`，任务包才成为开发阶段的唯一工作输入；此时才算设计阶段完成。

**没有完成人机设计对齐和任务包开工确认，不能进入开发。**

#### 机制八：设计包是本轮任务上下文，不成为永久工程文档

本轮设计围绕正式需求单进行工程探索、确认必要决策并拆为可执行任务。设计产物只保留让执行者能做事的必要信息：本轮要改什么、关键约束是什么、任务如何独立完成和验证。它不是项目知识库的重复副本。

验收后，本轮人类可读材料保留在 `docs/workflows/<task-id>/` 作为任务记录，但不进入后续任务的默认上下文。长期资产是否更新，先由独立 `knowledge-update-review.md` 逐项判定并经人审核；只有被批准、对未来任务仍有价值的内容，才收敛进长期知识：功能变化进 `docs/function/` 对应功能点，架构取舍与工程约束进 `docs/knowledge/`，已验收的 API、数据与跨系统契约变化分别进 `docs/knowledge/api/`、`data/`、`integration/`。

### 2.3 具体实现手段

第一版采用项目内 Markdown。初始化时先生成路径行组成的全量`文件树.md`骨架，再由分区 subagent 按实际阅读回写每项真实用途，随后一次写完`项目总览.md`（含软件设计架构）、`业务入口.md`、默认组件图的 `.puml` 与面向人的 `.svg`、识别到的工程模块、`docs/function/` 的模块和功能点、以及项目 Rules；只有 workflow 保持按需求创建。所有模板由结构 allowlist 校验标题、章节、表头和 `<...>` 声明，完成态不得保留占位或泛化用途。文件树在初始化、创建知识条目和创建 workflow 后刷新；自然语言意图由 `harness-orchestrator` 与阶段 Skills 装配到相应入口。

### 2.4 取舍

不以“文档完整度”衡量工程知识库，而以它是否减少不必要的源码探索、是否避免已知误判来衡量。第一轮宁可完整列出文件树，也不漏掉项目可见文件；长期说明则保持高价值、低频变化和按需生长。高频变化、源码可直接获得的内容不再重复维护。

## 三、开发阶段

### 3.1 开发流程

**在链路中的位置**：承接人已确认开工的任务包，将每个任务实现为可验证代码变更，并为后续评审、合并与发布提供机器可复核的结果。

**承上**：读取已放行的任务包、设计结论和任务所指向的目录/文件路径；实际代码、接口定义和配置仍是实现的第一事实。

**启下**：输出通过自动验证的代码变更与本轮交付汇总，交给代码评审阶段；不要求人逐任务盯进度或解释实现细节。

### 3.2 机制确认

#### 机制一：任务包一次性自主执行，不做人为分段交接

人确认任务包开工后，AI 将任务包作为一轮完整开发单元执行：共同约定已定的任务并行推进，在真实集成点收口；不是完成一个任务后等待人确认或再领取下一个任务。开发过程中不设置“高代价动作”的人工审批点，也不要求人逐任务确认；是否安全、正确，应优先由可执行的工程门禁判断，而不是由人凭感觉判断。

AI 只有在整轮开发后仍无法解决客观失败时，才在本轮交付汇总中报告失败证据与未完成项；这不是请求人工逐步接管，而是为后续处理保留事实。

#### 机制二：以 TDD 驱动实现，用机器门禁代替人工盯守

AI 对每项任务先依据任务验收标准编写或更新自动化测试，让测试先表达“完成应该是什么样”；随后实现代码，使测试通过，并在需要时重构。这是默认的 TDD 节奏：

```text
验收标准
→ 先写失败的测试
→ 最小实现使测试通过
→ 重构且保持测试通过
```

任务包中的并行任务可以同时采用该节奏。所有任务在真实集成点合并后，AI 对**整轮任务包**统一执行项目已有、可运行的门禁：

```text
格式化 / 静态检查（如 formatter、lint）
编译或类型检查
任务验收标准对应的自动化测试
项目全量测试
构建验证
```

项目没有某类命令时，不伪造“通过”，而是在交付汇总中明确“项目未配置该门禁”。门禁失败时，AI 应读取失败输出并自行定位、修复、重跑；整轮门禁未全部通过，不得展示为可验收结果。

所有门禁通过后，AI 将任务验收结果、版本摘要、Diff 概览和未配置门禁（如有）集中展示给人。人完成最终复核并明确确认后，AI 才提交 MR。这个人工动作是开发阶段的**硬闸口二**：人确认的是“验收结果是否足以代表本轮任务完成”，不是逐文件参与开发实现。

具体工具、命令、是否存在安全扫描及其配置方式，留到实现阶段按项目实际技术栈确定；本机制只规定：**门禁必须是可执行、可复核的证据，不是 AI 的口头自述。**

## 四、代码评审阶段

### 4.1 开发流程

**在链路中的位置**：承接已经人工验收并提交的 MR，由独立评审者检查代码是否正确实现需求、遵守已确认设计与工程约束，并将可复核的问题清单交给后续修复或报告确认。

**承上**：读取 MR Diff、正式需求及验收标准、设计结论、任务包，以及开发阶段的测试与门禁结果。不读取开发 AI 的对话、思考过程或自证性总结。

**启下**：无阻断问题时输出结构化评审结论，供后续合并报告与人工确认消费；有阻断问题时返回开发阶段修复、重跑门禁并更新 MR，然后重新评审。

### 4.2 机制确认

#### 机制一：独立、只读的代码评审，与实现和修复彻底分离

代码评审必须由新上下文中的独立 `code-reviewer` SubAgent 执行，不允许开发 AI 通过“切换角色”或追加一段评审提示词审查自己的实现。`code-reviewer` 只拥有读取代码、读取 Diff 和执行只读检查所需的最小工具权限，不拥有 `Write` / `Edit` 等文件修改权限。它只能返回评审结论，不得顺手修改被评审代码。

评审输入固定包含：

```text
MR Diff
正式需求及业务级验收标准
设计结论
任务包
开发阶段的测试与门禁结果
```

评审问题只分三级：

| 级别 | 定义 | 流程效果 |
| --- | --- | --- |
| `BLOCKER` | 有明确证据表明实现违反需求、设计契约，或存在功能错误、回归风险或严重工程问题 | 评审不通过，必须修复并重新评审 |
| `WARNING` | 存在风险或取舍，但证据不足以判定为错误 | 不自动阻断，进入报告供人确认 |
| `INFO` | 非必要的改进建议 | 不阻断合并，也不进入自动修复循环 |

自动判定规则是：存在任意 `BLOCKER` 则评审不通过；没有 `BLOCKER` 则可进入后续报告确认。

`BLOCKER` 的处理闭环为：

```text
code-reviewer 输出 BLOCKER 清单
→ 开发 AI 修复
→ 重跑整轮开发门禁
→ 更新 MR
→ 启动全新评审上下文复审
```

该自动修复—复审循环最多执行两轮。两轮后仍存在 `BLOCKER`，或修复需要扩大原任务范围、修改已确认需求/设计/共享契约、涉及不可逆操作或引入新的高风险问题时，立即停止自动循环并交由人处理。

#### 机制二：安全扫描是按项目能力启用的确定性分支

安全问题扫描不另外定义一个“安全评审 AI”，而是优先调用项目已经配置并可执行的确定性安全工具，例如静态应用安全检查、依赖漏洞检查和密钥/凭据泄漏检查。该分支与 `code-reviewer` 针对同一 MR 快照独立运行，两路结果最终汇入同一份评审报告。

工具负责检查可确定匹配的已知安全模式；`code-reviewer` 同时从需求、设计和代码语义中检查工具难以发现的业务安全问题，例如越权、租户隔离缺失和敏感数据暴露。

安全扫描是按项目现有能力启用的可选分支：项目有已配置工具时必须执行；项目没有配置时，在评审报告中明确标记“项目未配置安全扫描”，不伪造“扫描通过”，也不为了凑齐流程临时安装未经项目确认的扫描器。

安全扫描结果统一映射到与代码评审相同的三级结论：

| 情况 | 评审结论 | 流程效果 |
| --- | --- | --- |
| 项目未配置安全扫描工具 | 记录为“未配置”，不等同于扫描通过 | 不自动阻断，进入报告供人确认 |
| 项目已配置工具，但执行失败、超时或配置损坏 | `BLOCKER` | 检查实际未完成，必须排除故障并重新执行 |
| 扫描确认存在密钥/凭据泄漏，或 `Critical` / `High` 风险 | `BLOCKER` | 必须修复并重新扫描、复审 |
| 扫描发现 `Medium` 风险 | `WARNING` | 不自动阻断，进入报告供人决定 |
| 扫描发现 `Low` / `Info` 风险 | `INFO` | 仅记录，不进入自动修复循环 |

如项目已有经过确认的忽略清单或风险豁免，扫描分支可以沿用，但必须在报告中保留被忽略项、豁免理由及来源，避免把“已接受风险”误写成“没有风险”。

#### 机制三：在代码合并报告中保存需求—实现—验证映射

每条正式需求或业务级验收标准，都必须在代码合并报告中对应到实际实现位置和验证证据，使人工评审者能够直接回答“这条需求由哪里实现、由什么证明”。最小映射字段为：

| 字段 | 含义 |
| --- | --- |
| 需求/验收标准 | 被实现和验证的正式条目 |
| 关联任务 | 承接该条目的开发任务包条目 |
| 实现位置 | 相关模块、文件或关键代码位置 |
| 验证证据 | 对应测试、质量门禁或人工验收结果 |
| 状态 | 已覆盖、部分覆盖、未覆盖或不适用 |

第一版不为该映射单独建设数据库、台账或新服务，以代码合并报告为单一呈现载体。若某条需求为“部分覆盖”或“未覆盖”，不得生成可直接合并的结论，必须明确差距并交由人处理。

#### 机制四：代码合并报告必须是可独立阅读的成型文本

代码合并报告不是零散的 MR 评论、若干状态标签或原始日志拼接，而是一份结构完整、可独立阅读的正式文本，面向最终审核 Pull Request / Merge Request 的人员。报告应把本次变更的目标、需求覆盖、实现范围、验证结果、独立代码评审、安全扫描、未解决风险和合并建议组织成连贯结论；原始日志和明细作为证据引用，不直接替代报告正文。

报告只提供事实汇总、风险说明和合并建议，不代替人工批准，也不自行执行合并。

报告以 Markdown 文件形式落盘到 `docs/workflows/<task-id>/merge-report.md`；同一份报告的正文或访问链接同步发布到 MR 页面，供审核者直接阅读。文件与 MR 展示内容必须由同一次生成产生，不允许形成两份需要分别维护的报告。

报告正文采用固定结构：

1. **基本信息**：MR、分支、Commit、需求编号和生成时间；
2. **本次变更**：变更目标、主要修改和影响范围；
3. **需求实现与验证**：需求—任务—实现位置—验证证据—覆盖状态；
4. **测试与质量门禁**：执行项、结果和证据位置；
5. **独立代码评审**：`BLOCKER`、`WARNING`、`INFO` 及其处理状态；
6. **安全扫描**：工具、执行状态、发现项和风险豁免；
7. **遗留问题与风险**：尚未解决的问题、影响和建议；
8. **合并结论**：建议合并、不建议合并或需要人工判断。

每份报告必须绑定生成时的 MR Commit。只要 MR 出现新的代码提交，原报告就自动失效，必须基于新快照重新执行相关检查并生成新报告。

#### 机制五：长期知识与 Rules 的回写必须先经独立审核

代码合并报告的职责是说明“本次代码是否可以合并”；长期知识更新审核单的职责是说明“本轮代码和设计改变了哪些长期事实、哪些必须更新、哪些无需更新及其依据”。两者读者、审核问题和失败后动作不同，**不得合并为同一份文件**。

独立审核单固定落盘为 `docs/workflows/<task-id>/knowledge-update-review.md`，在独立代码评审通过后、MR 最终批准前生成。它必须逐项覆盖：`项目总览.md`、`文件树.md`、`业务入口.md`、`architecture/`、`modules/`、相关 `docs/function/` 功能点及 `.codebuddy/rules/`。每项只允许判定为：`需要更新`、`无需更新`、`待人裁定`；后两者均需附依据，存在任何`待人裁定`时不得进入最终 MR 审核。

审核单必须给出每项“需要更新”的目标文件、更新草案、被替代内容、来源证据及保留理由。AI 先提交审核单，不得直接改写长期资产；负责人审核通过后，AI 才在同一 MR 分支写入已批准更新，并在审核单中记录实际改动路径与 Commit。`.codebuddy/rules/` 仅在形成跨任务、长期稳定且必须遵守的规约时允许更新；本轮局部实现细节不得升格为 Rules。

这样，知识回写是独立、可审计的审核交付物，而不是合并报告末尾的一段附注；MR 页面可以同时链接两份文件，但不要求审核人从合并报告中推断知识资产是否更新。知识回写会产生新的 MR Commit，因此原评审结论与合并报告自动失效；必须基于最终快照重新执行独立评审，再生成绑定最终 Commit 的合并报告。

#### 机制六：合并批准沿用代码平台的原生人工审核

AI 的评审阶段职责止于生成并发布代码合并报告。报告之后不建设“AI 审批”、额外确认页面或新的人工交互协议，而是直接沿用 GitHub、GitLab 等代码平台已有的 Pull Request / Merge Request 人工审核流程。具备相应权限的审核人在平台上批准或退回 MR；流程编排只读取平台上的审核结果以推进状态，不替代审核人作出决定。

人工批准只对报告绑定的 MR Commit 有效。出现新提交时，原报告与原批准均视为失效，必须重新检查、生成报告并按平台规则重新审核。

### 4.3 具体实现手段

第一版定义项目级 `code-reviewer` SubAgent，建议载体为：

```text
.codebuddy/
└── agents/
    └── code-reviewer.md
```

开发主流程在 MR 已创建或更新后，组装结构化评审输入包并自动拉起该 SubAgent。SubAgent 在独立上下文中返回结构化问题清单；由主流程保存评审结果并推进状态，不由评审者直接改写仓库文件。

如运行环境不支持自动拉起 SubAgent，则降级为由用户在新会话中启动同一评审流程并读取已落盘的评审输入包；该方式只是兼容性降级，不作为默认路径。

主流程同时检测项目已声明的安全扫描命令或适配器。如果存在，它与 `code-reviewer` 并行执行；如果不存在，主流程只记录未配置状态。扫描命令、适配器与严重级映射按项目技术栈确定，不在通用流程中写死特定产品。

### 4.4 取舍

评审不在开发 AI 的原会话中进行，也不让 `code-reviewer` 直接修复问题。这会多一次上下文组装与 Agent 调用，但换来实现判断与评审判断的真正隔离，避免开发者的思路、假设和自证结论污染评审。

第一版不另外定义专用 `code-fixer` SubAgent；阻断问题交回原开发 AI 修复。只有当修复行为已稳定复用、需要单独的权限或上下文隔离时，才讨论拆出专用修复 Agent。

安全扫描采用“项目声明什么就执行什么”的能力发现方式，而不在通用体系里强制绑定某一款工具。代价是冷启动项目可能只有 AI 的业务语义安全检查，但这比临时引入未经配置、无基线且不可稳定复现的扫描器更可控。

> 代码评审阶段已确认完毕；下一步进入发布阶段。

## 五、发布阶段

### 5.1 责任边界

第一版 Harness 的业务流程责任截止于 MR 完成评审并合并。合并后的测试、构建、制品生成和部署继续由项目现有 CI/CD 配置自动执行，Harness 不创建额外“交付包”，不修改或复制流水线，也不直接操作生产环境。

这里所说的制品（如 JAR、Docker 镜像或前端静态文件）属于 CI/CD 的构建结果，不是 Harness 额外生成的文档。变更说明、影响范围和验证证据已经包含在 Commit、MR 描述及代码合并报告中，合并后不再重复生成发布说明。

### 5.2 第一版流程终点

```text
MR 审核通过并合并
→ 项目现有 CI/CD 按自身触发规则运行
→ Harness 核心业务流程结束
```

读取流水线结果、分析失败日志或辅助回滚均不纳入第一版。只有在后续明确需要接入特定 CI/CD、日志或监控平台，并具备稳定接口和权限边界时，才单独扩展这些能力。

## 六、运维阶段

第一版不进入运维阶段。日志异常分析、线上 BUG 定位、告警研判与回滚辅助均保留为未来扩展点，不作为本次 Harness 的交付责任。

## 七、横切机制 A · 状态文件

### 7.1 粒度与职责

第一版按“一项正式需求到一个 MR”为单位维护一份状态文件。需求拆出的多个开发任务属于同一次流程实例，统一记录在这份文件中；不再额外维护项目级总状态文件或每个子任务各自的状态文件。

状态文件是流程调度的可信进度存档，只回答当前阶段、已完成步骤、产物位置、门禁结果、阻断原因和下一合法动作。它不保存对话历史，也不复制需求、设计、任务包或报告正文，只保存这些产物的引用及与流程推进有关的最小事实。

任何新会话、主调度器或被授权的 Agent，都应能够只读取状态文件及其引用产物，在不依赖原对话上下文的情况下理解当前进度并继续执行。

### 7.2 文件格式

状态文件采用 JSON，并用 JSON Schema 校验字段、枚举值和必填关系。选择 JSON 是为了让状态解析、合法转换判断和门禁阻断能够由确定性脚本完成，而不是依赖 AI 理解自然语言。文件必须保留简短的 `summary` 与 `next_action` 字段，确保人无需阅读全部结构也能知道当前进度和下一步动作。

### 7.3 存储位置

完整项目布局见 0.4 节。本机制只涉及其中的流程实例部分：

```text
docs/workflows/<task-id>/            # 人读：本轮 Markdown 产物
└── README.md、requirement.md、design-alignment.md、
    design-decision.md、development-contract.md、task-package.md、
    development-summary.md、knowledge-update-review.md、merge-report.md

.codebuddy/workflows/<task-id>/      # 机器跑：默认 gitignore
├── state.json
└── evidence/                         # 运行时证据存储；真实载体、文件数量和命名不预设
```

目录按读者而不是按生命周期划分：程序员需要阅读的 Markdown 全部进入可见的 `docs/workflows/<task-id>/`；机器驱动流程所需的 `state.json`、结构化检查结果和原始日志进入 `.codebuddy/workflows/<task-id>/`。`state.json` 的 `artifacts` 字段引用对应 Markdown；`artifacts.evidence_refs` 以逻辑证据 ID 引用机器证据，实际位置由运行时记录。

`docs/workflows/README.md` 是任务总索引，至少按任务名称、状态和创建时间链接到每个任务目录；任务目录内的 `README.md` 再导航到本任务各份文档。程序员无需记住内部 ID，也不需要进入隐藏目录寻找文档。`docs/knowledge/` 与 `docs/function/` 只保存经过抽象、对未来任务仍然有效的长期知识。

`.codebuddy/workflows/` 默认加入 `.gitignore`，不提交到业务代码分支；`docs/` 纳入项目正常文档与 Git 管理。这里的忽略仅针对 `.codebuddy/workflows/` 机器运行态，不得扩大为忽略整个 `.codebuddy/`：`.codebuddy/settings.json`、`.codebuddy/harness.json`、`.codebuddy/onboarding-checklist.json`、`.codebuddy/rules/` 与 `.codebuddy/agents/` 是管理员确认后的团队共享配置，必须随仓库提交。合并报告还要把同一正文或访问链接同步到 MR 页面，避免审核人必须先检出分支才能阅读。

工作流内部 ID 的格式固定为 `wf-<YYYYMMDD>-<6 位小写字母或数字>`。日期表示创建日，随机短码保证同日多任务不冲突；ID 创建后不再变化。目录名只使用 ASCII，不写需求标题，也不记录或拼接外部工单号。

第一版保证同一项目工作区内跨会话恢复，不承诺跨电脑同步；若未来需要团队成员在不同机器接力，再为该目录增加共享存储适配器，而不是把运行状态混入业务提交。

**模板与证据口径**：模板只定义字段契约，所有运行时动态值使用 `<...>` 占位符。不得把某个任务标题、需求 ID、代码路径、接口参数、命令、测试名、断言数量、平台名称、状态结论，或某一份日志、某类文件后缀、证据数量写成模板示例。这类 few-shot 式例子会把偶然的项目事实和运行形态暗示成规范，诱导 AI 在不同项目中虚构或强行套用。完整业务场景只能放入独立的全流程模拟，并明确其非模板性质。

证据统一以运行时生成的逻辑引用 `EV-<runtime-generated-id>` 表示，并至少记录：证据类型、产出方、证明对象、对应 Commit 和可访问位置。可访问位置可以是一份文件、多份文件的索引、平台链接或内联结构化结果；只有运行时和状态文件知道真实承载形态，人读文件只引用证据 ID 与证明含义。

### 7.4 如何定位与读取

状态文件不是由模型“自动记住”，而是在启动或恢复 Harness 流程时由确定性入口脚本读取：

```text
进入项目并启动 Harness
→ 定位项目根目录
→ 扫描 .codebuddy/workflows/*/state.json
→ 校验 JSON Schema
→ 选择本次 task-id
→ 返回当前摘要、下一动作及必须读取的产物路径
→ AI 按路径读取必要产物并继续
```

选择规则固定为：用户或调用方显式提供 `task-id` 时直接读取对应文件；未提供时，如果只有一个未完成任务就自动恢复；如果存在多个未完成任务则列出任务摘要并要求选择，不允许 AI 猜测。普通问答不会加载所有状态文件，只有进入 Harness 流程时才执行上述读取。

主流程读取完整状态；SubAgent 不自行扫描运行目录，只接收当前职责所需的结构化输入包，避免把无关流程上下文带入独立评审等任务。

### 7.5 核心字段

状态文件至少包含：

```json
{
  "schema_version": "<schema-version>",
  "task_id": "<workflow-id>",
  "title": "<task-title>",
  "stage": "<current-stage>",
  "step": "<current-step>",
  "status": "<current-status>",
  "summary": "<human-readable-current-summary>",
  "next_action": "<next-legal-action>",
  "artifacts": {
    "<artifact-key>": "<artifact-path>",
    "evidence_refs": "<runtime-generated-evidence-reference-array>"
  },
  "tasks": "<runtime-generated-task-state-array>",
  "gates": "<runtime-generated-gate-results>",
  "approvals": "<runtime-generated-approval-state>",
  "mr": "<mr-state-when-applicable>",
  "blockers": "<runtime-generated-blocker-array>",
  "created_at": "<timestamp>",
  "updated_at": "<timestamp>"
}
```

`stage` 表示需求、设计、开发、评审或完成；`step` 表示当前具体步骤；`status` 只允许 `pending`、`running`、`waiting_human`、`blocked`、`completed`、`failed`。其余字段分别引用本轮产物、任务进度、质量门禁、人工确认、MR 状态和阻断原因。状态文件不保存原始对话、AI 思考过程或大段命令日志。

### 7.6 状态转换与写入

Agent 不直接任意编辑 `state.json`，而是调用统一状态管理脚本。脚本读取旧状态、校验目标转换、检查所需产物/门禁/人工确认，然后使用临时文件原子替换旧文件。SubAgent 只返回结果，由主流程验证并写入状态。

流程只允许沿已定义步骤前进或按明确回退规则返回。发现跳步、缺少必需产物、门禁失败、人工确认缺失、JSON 无效或非法转换时，脚本必须保留上一份有效状态并停止，不允许 AI 用自然语言解释后强行继续。

每个步骤完成后更新一次状态，不记录中间思考。MR 合并后将状态标记为 `completed`；第一版保留已完成目录，不自动删除，也不另建数据库、项目总状态文件或事件流水系统。

> 状态文件机制已确认完成；下一步进入质量门禁的横切设计。

## 八、横切机制 B · 质量门禁

### 8.1 已确认的实现边界

质量门禁采用同一套项目门禁定义，并由 CodeBuddy 会话与项目现有协作平台分别执行：

```text
CodeBuddy PreToolUse Hook
→ harness.json 中声明的项目门禁
→ 项目现有 MR/CI required check（接入时适配）
```

- **CodeBuddy 层**：Plugin 提供 `PreToolUse` Hook，在 AI 调用工具前检查并以 `continue:false` 阻断违规操作；该层只覆盖 CodeBuddy 会话内的工具调用。
- **项目门禁层**：运行时按 `.codebuddy/harness.json` 顺序执行项目已有的构建、测试和扫描命令；是否接入本地 Hook 由项目自行决定。
- **远端平台层**：项目可把同一门禁命令接入已有 CI，并配置为 MR 合并所需的 required check；Plugin 第一版不生成特定平台模板。

项目构建、测试、扫描及门禁命令由纳入 Git 管理的 `.codebuddy/harness.json` 声明；Plugin 承载通用 Hook、解析与执行能力。技术设计第 2 节列出的共享契约不得加入忽略规则，并由管理员确认后提交 Git。

门禁失败必须返回具体失败项、实际执行命令、必要证据位置和下一步修复动作，不得只输出“违反规范”等不可操作信息。

### 8.2 第一版确定性实现契约

`.codebuddy/harness.json` 固定使用 `schemaVersion: "1.0"` 和 `adapterVersion: "0.8.0"`。顶层包含 `protectedContracts` 与 `gates`；`gates` 下定义 `preCommit` 和 `ci` 两个检查数组。每个检查包含 `id`、`command`、`args`、`cwd`、`timeoutSeconds` 和 `required`。命令与参数分离并以 `shell:false` 执行，`cwd` 不得逃逸项目根目录。

检查按配置顺序串行执行。默认超时 300 秒；初始化器识别出的 lint/build 默认 600 秒，测试默认 900 秒。`required:true` 的检查失败、超时、命令不存在或配置无效时立即失败关闭；非必需检查保留结果但不阻断。第一版不做并行执行和风险分档，以结果顺序稳定、错误定位明确为优先。

Plugin 注册两个 Hook：

- `SessionStart`：检查项目是否完成接入，并提示唯一可恢复任务；多个未完成任务时只列出，不自动选择。
- `PreToolUse`：匹配写入与命令类工具，阻止直接改写运行态 `state.json`、阻止把整个 `.codebuddy/` 或共享契约加入忽略规则，并在明确执行提交动作前运行已配置的 `preCommit` 门禁。它不建立 `constraints/` 或路径注入轨。

命令入口由 Plugin 提供；业务仓库不复制 `.codebuddy/scripts/harness.mjs`。CI、本地 Hook 或其他平台集成只能按项目确认后的适配方式调用门禁，不把某个平台或脚本路径写死为默认产物。

每次门禁输出结构化结果：`schemaVersion`、`profile`、`ok`、`checks`、`finishedAt`。单项检查记录 `id`、实际命令与参数、状态、退出码、耗时和截断后的标准输出/错误输出。主流程为每个可引用结果生成逻辑证据 ID，并写入 `artifacts.evidence_refs`；报告引用该 ID 与其证明含义，不写死真实文件名、文件后缀或证据数量。原始大日志不写入状态正文。

### 8.3 已实现交付

第一版按上述契约实现为 CodeBuddy Marketplace：市场名固定为 `ai-market`，根目录 `.codebuddy-plugin/marketplace.json` 是市场入口；插件名固定为 `coding-harness`，`plugins/coding-harness/` 是 Plugin 本体。自然语言通过六个聚焦 Skills 路由，四个 `/coding-harness:<command>` 作为备用入口。Plugin 还包含一个只读独立评审 Agent、两个 Hooks、状态与门禁运行时、业务仓库初始化模板、JSON Schema 和自动化测试。此清单必须随实际包结构同步，不能把未生成的 Git/CI 文件写成已交付。

管理员共享配置必须写入业务仓库的 `.codebuddy/settings.json` 并由 Git 跟踪；共享配置使用团队可访问的 Git Marketplace URL，不得写入个人电脑的本地绝对路径。具体安装与发布步骤见 `docs/INSTALL-CODEBUDDY-IDE.md`。

## 九、横切机制 C · 经验账本与升格

### 9.1 第一版职责

经验账本不能把单次任务的具体经过、具体修复步骤或局部结论直接作为后续模型必须遵循的提示词。候选经验必须先提炼为可迁移的抽象要点，并明确适用条件与边界；否则具体案例会诱导模型机械套用，造成提示词过拟合。

第一版由 `knowledge-update-review` Skill 在合并前审核长期知识与 Rules 是否需要更新。它只从已完成任务的正式产物提取可迁移事实，不读取或复制原始对话、模型思考和整段终端日志。单次偶发现象保留在任务文档，不自动成为长期规则。

### 9.2 抽象与审核契约

候选经验固定包含：问题模式、原因、建议原则、适用条件、反例、边界、验证方法、证据任务和推荐载体。抽象时删除业务专名、一次性路径、具体工单号和只对当前实现成立的操作步骤。无法写清适用条件与边界的候选不得升格。

候选必须经人审核后才能进入长期项目知识。升格链共三级，按“越往右越贵、越强制”排列，每级都有明确准入判据：

| 级别 | 载体 | 准入判据 | 违反后果的性质 |
| --- | --- | --- | --- |
| 一 | `docs/knowledge/` | 解释性、需要判断、能减少探索成本 | 多花时间，不出错 |
| 二 | `.codebuddy/rules/` | 已成团队规约，且可用一句`适用场景`描述何时适用 | 不规范、风格不一致 |
| 三 | Hook / 质量门禁 | 不可违反，且可用确定性检测判定 | 生产事故级或不可逆 |

- 只有能明确回答“违反它会造成什么后果”，且能够确定性检测的规则，才允许升到第三级。后果仅为“不好看”“不规范”“效率低”的规则一律停在第二级。
- 无法确定性检测但需要团队遵守的约束仍写入匹配的 Rules，并通过设计、测试和独立评审核对；不另建路径注入目录。
- 同一规则反复被违反且出现可确定性检测手段时，应从 Rules 升格为 Hook 或质量门禁。
- 原始任务流水、单次修复命令和未经验证的局部结论永不进入任何一级默认提示词。

第一版不做自动频率统计、自动批准或自动修改全局规则。其触发条件仍按 0.3 节执行：经验条目超过 30 条后再评估频率统计，项目知识超过 50 条后再讨论分层、成熟度与衰减。

## 十、横切机制 D · 用户引导层

### 10.1 决策

Plugin 增加统一主入口 `/coding-harness:start`。普通用户不再需要理解 `init`、`doctor` 和 `status` 的区别；主入口根据项目接入、活动任务数量和配置有效性自动路由。高级命令继续保留用于维护与排障。

所有 Harness 阶段性响应采用“当前阶段、已经完成、等待确认或阻断原因、确认或修复后进入”的固定导航结构。结构化 JSON、命令、绝对路径和完整日志属于证据层，默认不展示，只有失败定位、审核或用户主动请求详情时展开。

### 10.2 技术边界

CodeBuddy `SessionStart` Hook 的 stdout 进入 Agent 上下文，不等于用户可见欢迎页。因此 Hook 只使用结构化 `additionalContext` 注入导航规则，不承担主动弹窗。欢迎与下一步提示由 `/coding-harness:start`、接入完成处理和各阶段 Skill 的输出契约负责。

第一版由 `harness-orchestrator` Skill 与 `SessionStart` Hook 分担引导：Skill 识别自然语言意图并调用受控运行时，Hook 只报告接入状态和未完成任务。两者不直接改写状态、不执行 Git、不代替项目负责人确认；与 Harness 无关的普通问答不强制进入流程。

完整交互和验收契约见 `docs/specs/2026-09-07-guided-user-experience-design.md`。

## 十一、横切机制 E · 上下文装载

### 11.1 为什么单独立章

第一章到第十章定义了产物写什么、放在哪、以及 AI 应该按什么顺序读取。但“应该按什么顺序读”是**读取顺序**，不是**装载机制**。缺少装载机制时，2.2 机制四那条渐进披露路径只是一条对 AI 自觉性的期望。

这与 8.1 已确立的原则直接冲突：**门禁必须是可执行、可复核的证据，不是 AI 的口头自述。** 同一标准必须同样适用于上下文——对代码质量要求机器强制，对上下文装载却依赖自觉，是标准适用上的不一致。本章补齐这一层。

本机制的验收判据是：**把 `业务入口.md` 改坏，AI 的行为是否随之改变。** 若不改变，说明它从未被真正装载。

### 11.2 三种职责与准入判据

上下文与确定性检查分为三种职责，不建立额外的路径注入目录：

| 职责 | 决定什么 | 实现方式 |
| --- | --- | --- |
| 常驻入口 | AI 每轮必须知道的项目入口与流程红线 | `CODEBUDDY.md` |
| 按需事实与规约 | 当前任务需要读取的知识和 Rules | Skills 根据用户意图、任务类型和明确的适用场景装配 |
| 确定性阻断 | 能由程序客观判断的违规操作 | `PreToolUse` 或项目门禁 |

不能确定性判断的团队约束写入匹配的 Rules，并在设计、测试和独立评审中核对；能确定性判断的违规才进入 Hook 或门禁。第一版不建立 `constraints/`、路径 glob 注入清单或第二套规则体系。

### 11.3 常驻层：`CODEBUDDY.md` 与预算分配

常驻层唯一载体是项目根目录的 `CODEBUDDY.md`，由 CodeBuddy 原生自动加载，**不占用 Hook 注入通道**。它与 10.2 中 `SessionStart` Hook 的 `additionalContext` 职责严格分工：

| 通道 | 内容性质 | 变化频率 |
| --- | --- | --- |
| `CODEBUDDY.md` | 项目静态事实与不可违反约定 | 低频，随项目结构变化 |
| `SessionStart` 的 `additionalContext` | 当前任务状态、下一动作、导航规则 | 每次会话不同 |

两者不得重复注入同一内容。静态约定只写 `CODEBUDDY.md`；动态状态只走 `additionalContext`。

`CODEBUDDY.md` 硬上限 200 行，建议预算分配如下。行数是上限而非配额，用不满是正常结果：

| 段落 | 行数 | 内容 | 准入理由 |
| --- | --- | --- | --- |
| 按需层索引 | 40 | 每个可加载文档或目录一行：路径 + 什么时候读 | 最大配额。按需层若无常驻索引则等于不存在 |
| 不可违反的工程约定 | 40 | 仓库契约不得忽略、产物按读者分目录、代码是实现第一事实 | 违反直接破坏接入完整性 |
| 三道闸口与流程红线 | 25 | 未确认开工不进开发、门禁未过不展示为可验收、不得改写运行态状态文件 | 违反直接跑错流程 |
| 术语与命名 | 20 | 业务术语、模块中英对照、`wf-` ID 格式 | 猜错会连带产物命名全错 |
| 项目是什么 | 15 | 定位一段话 + 技术栈 + 启动/构建/测试命令 | 对齐 `/init` 的最低效果 |
| 编码规约指针 | 10 | 指向 `.codebuddy/rules/`，不展开内容 | 风格偏好属按需层 |
| 留白 | 50 | 项目自行扩展 | 保留演进空间 |

**编码风格偏好不进常驻层。** 风格错误的后果是“不好看”，不会导致返工；而结构未知、按需层入口未知、闸口规则未知会直接导致流程跑错。业界公开的个人编码风格文档（如极简主义、反过度抽象一类主张）可作为 `.codebuddy/rules/code-style.md` 的参考来源，但不得直接充当常驻层内容：它们本质是个人审美偏好且与技术栈强绑定，套进有历史包袱的业务仓库时，其反对的抽象可能恰是团队既有架构约定，反而制造冲突。

初始化器生成 `CODEBUDDY.md` 草案后必须报告实际行数与各段占比；超过 200 行时不得静默截断，应提示项目负责人裁剪并给出建议下沉到哪一层。

### 11.4 按需层：适用场景声明是唯一匹配依据

按需层包含四类内容：

| 内容 | 位置 | 典型触发时机 |
| --- | --- | --- |
| 功能知识 | `docs/function/` | 需求定位到功能点后 |
| 架构与工程模块说明 | `docs/knowledge/architecture/`、`docs/knowledge/modules/` | 探索跨服务关系、架构取舍、已知易误判点 |
| 团队规约 | `.codebuddy/rules/` | 新建模块、重构、提交、写测试、评审 |
| 项目导航 | `docs/knowledge/` 下的项目总览、文件树、业务入口 | 定位陌生模块与文件 |

`.codebuddy/rules/` 固定包含五份：`architecture.md`、`engineering.md`、`testing.md`、`api-and-data.md`、`commit-and-mr.md`。`code-reviewer` 只消费按任务装配后的匹配 Rules，不另建评审专用 Rule。

**每份按需层文档的首行必须声明 `适用场景:`，且必须写明可判定的触发条件，不允许写“通用”“随时”。** 这是描述匹配唯一可依据的信号；缺少该声明的文档视为未纳入按需层，初始化自检应报告为缺陷。

```text
适用场景: 新建模块、跨模块重构，或需要确定命名与目录归属时
```

按需层是概率性装载，不做可靠性承诺。凡是“漏读就会出事”的内容，不允许留在按需层，必须按 11.2 判据升到强制层或下沉为门禁。

### 11.5 确定性阻断的准入门槛

`PreToolUse` Hook 只处理能够由输入或项目状态确定判断的违规，例如直接改写运行态 `state.json`、将共享契约加入忽略规则，以及明确的提交动作未通过已配置门禁。命中时返回可执行原因和修复动作。

无法仅凭工具输入或确定性检查判断的业务语义，不允许伪装成物理阻断，也不通过路径注入片段实现；它必须写入五份 Rules 中职责匹配的文件，并由任务上下文、设计、测试和独立评审共同验证。

### 11.6 资产布局

```text
CODEBUDDY.md                      # 常驻层，≤200 行，Git 跟踪
.codebuddy/
└── rules/                        # 按需层规约，Git 跟踪
│   ├── architecture.md           # 首行须有 适用场景:
│   ├── engineering.md
│   ├── testing.md
│   ├── api-and-data.md
│   └── commit-and-mr.md
```

上述路径全部属于团队共享的仓库契约，必须纳入 Git 管理，AI 与初始化器不得将其加入任何忽略规则。初始化自检应验证 `CODEBUDDY.md` 未超行数上限，且 `rules/` 精确包含五份文件并均有 `适用场景:` 声明。

本节只列上下文装载相关资产；按需层实际承载的知识目录（`docs/knowledge/`、`docs/function/`）与完整项目布局见 0.4 节。

### 11.7 取舍

**取舍一：常驻层是否直接采用业界公开的编码风格文档？**

不直接采用，仅作为 `rules/code-style.md` 的参考来源。原因见 11.3：风格偏好的违反后果不足以占用每轮预算，且与技术栈强绑定，可能与既有架构约定冲突。

**取舍二：是否建立路径约束注入层？**

不建立。团队语义约束进入五份 Rules，能确定性检查的违规进入 Hook 或质量门禁，避免形成第二套规则目录和注入清单。

**取舍三：按需层是否引入向量检索以提高匹配质量？**

第一版不引入，与 1.4 取舍五保持一致，改用 `适用场景:` 显式声明 + 常驻层索引。原因是冷启动阶段显式声明比语义相似度更可控、可审查、可在自检中校验。代价是匹配召回不如语义检索，接受由常驻层索引兜底；按需层文档量增长到明显瓶颈后再单独讨论。

**取舍四：是否为三种职责建立统一的加载调度器？**

不建。常驻入口由 CodeBuddy 原生加载，按需内容由 Skills 装配，确定性违规由 `PreToolUse` 或项目门禁处理。引入统一调度器会叠加一层需要自行维护的抽象。
## 0.7.0 决策增补：动态初始化与 Markdown 结构合同

### 决策

1. 初始化由主 Agent 在会话内维护依赖图并分发 subagent：主 Agent 扫描全量路径，运行时先写路径骨架；每个可读路径只分配一个业务或工程阅读负责人；业务、工程和 Rules 任务按实际依赖动态启动，写入范围互不重叠。业务、工程、Rules subagent 直接写自己负责的长期文档；`文件树.md` 与组件 SVG 仅由运行时写入。主 Agent 只做调度、失败重派和确定性结构校验，不增加中间人工审核回合，subagent 与 CLI 不得回传全量扫描或源码全文。prepare、subagent 回写和 finalize 属于同一顶层编排会话；脱离会话的 CLI finalize 对已有长期资产按冲突处理，不能用未持久化的清单覆盖。
2. 文件树分两态：探索态只有路径行，完成态每行必须有基于阅读的具体用途。不得以“源码目录”“配置文件”“业务逻辑”“用途待确认”等套话代替事实；二进制、符号链接和不可读项必须标明类别或原因。
3. 统一 Markdown 结构由 allowlist 解析器验证。项目总览在技术栈之后固定包含“软件设计架构”；工程模块固定包含九个业务相关章节；功能描述固定包含元数据和五个章节；Rules 固定包含“必须遵守、相关知识入口、验证方式、更新门槛”。模板中的动态值只能使用声明的 `<...>`，完成态不得有未替换占位符，知识链接必须指向存在的相对路径。
4. 架构图同时保存 `component.puml` 和面向人的 `component.svg`。Git 是共享契约、文档版本和历史追溯的唯一依据；本版本不引入 digest、内容哈希、CAS 或额外初始化锁。
5. Hook 先检测有效 `.codebuddy/harness.json`。无配置的项目完全 no-op；只有孤立 checklist 或无效配置时 fail-closed 提示修复；有效 Harness 项目继续执行状态保护、共享契约忽略保护和 preCommit 门禁。

### 结果

0.7.0 的安装包、运行时、模板、Skill、技术设计和本 ADR 均以该合同为准。项目初始化完成时，除按需创建的 workflow 外，所有规范长期资产必须已写入真实内容；管理员只需通过顶层 Skill 的自然语言 checklist 确认并提交 Git，其他成员拉取同一提交即可。

## 0.8.0 决策增补：共同开发契约成为一等交接产物

### 决策

1. 在 `design-decision.md` 与 `task-package.md` 之间新增 `development-contract.md`；每个 workflow 固定为 README 加十份正文，共十一份文件。
2. `development-contract.md` 是所有开发任务共同读取的唯一契约正文。`design-decision.md` 只保存范围、边界、覆盖与取舍，`task-package.md` 只保存任务分解和契约 ID 引用，两者不得复制契约正文。
3. 契约按需装配 HTTP API、公共接口、数据、跨任务集成和共享行为五类模板。不适用的块不生成。普通状态、权限和行为结果写入验收标准；只有多个任务必须共同遵守的状态、权限、事务或并发语义才进入共享行为契约。
4. 设计阶段批准和进入开发的门禁都要求共同开发契约完成且结构合法；每个开发任务必须读取整份契约，并声明真实存在的负责与使用契约 ID。`design-decision.md`、`development-contract.md`、`task-package.md` 必须已提交且工作区干净，确认记录绑定该 Git 版本；确认后任一文件变化，或实现需要改变契约时，必须退回设计并重新确认任务包。不引入 Git 之外的内容摘要或版本机制。
5. 主模板、五类片段模板、技术设计和运行时使用同一组标题、表头与占位符 allowlist。版本历史继续只使用 Git，不增加 digest、内容哈希、CAS 或额外初始化锁。

### 结果

0.8.0 的安装包、运行时、模板、Skill、技术设计、HTML 和本 ADR 统一采用上述十一份 workflow 结构；0.7.0 中“共享契约直接写在任务包开头”的规则失效。
