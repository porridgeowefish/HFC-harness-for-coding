# 技术设计：流程模块、产物模板与交接协议

> **状态**：设计稿  
> **适用范围**：AI Coding Harness 的需求、设计、开发、评审、合并五段主链路  
> **不讨论**：Plugin 与 Skill 的顶层分发形态、模型选型、向量检索、并行 Worktree、运维 MCP  
> **核心目标**：每个节点有可检查的输入、固定的输出和明确的交接门槛；节点之间传递落盘事实，不传递对话记忆。

---

## 1. 设计边界

### 1.1 本文解决什么

本文只定义三件事：

1. **节点内部模板**：每个节点产物采用什么格式、有哪些必填字段、哪些字段由人确认；
2. **节点之间交接**：下游节点必须读取什么、上游满足什么条件才能放行、失败时回退到哪里；
3. **项目适配配置**：哪些是通用能力，哪些是项目事实，怎样让负责人只确认差异而非填写大表单。

### 1.2 不做什么

- 不建立额外的“上下文编排平台”；
- 不传递 Agent 的对话记录、思考过程或临时命令历史；
- 不建立 `constraints/`、路径注入或业务语义强制提醒机制；
- 不让 AI 自动修改顶层规则；
- 不把每份文档做成表单系统。Markdown 是人读界面，JSON 是机器状态界面。

### 1.3 四类信息的职责

```text
.codebuddy/rules/            顶层架构与开发规约：必须怎样做

docs/knowledge/              工程事实：工程现在如何组织、代码在哪里、架构为什么这样

docs/function/               业务事实：功能是什么、规则是什么、历史为什么如此

docs/workflows/<task-id>/    本轮事实：这次要做什么、怎样设计、怎样验证、结果如何
```

> `rules/` 是项目的宪法，不是可选风格建议。工程知识和业务知识用于理解事实；规则用于约束本轮方案和实现是否越界。

---


## 2. 文件布局与命名

```text
<project-root>/
├── .gitignore                     # 保留项目既有规则，并忽略 `.codebuddy/workflows/` 机器运行态
├── CODEBUDDY.md                  # 常驻入口：项目事实摘要、全局行为规则、规则索引、任务触发条件；≤200 行
├── docs/                         # 人读的长期知识与每轮流程档案；全部纳入 Git
│   ├── knowledge/                # 长期工程知识：回答“工程如何组织、代码在哪、架构为什么这样”
│   │   ├── 项目总览.md           # 项目用途、技术栈、启动/构建/测试入口、顶层模块与导航
│   │   ├── 文件树.md             # 全量可见文件/目录及一句话用途；由工具自动刷新
│   │   ├── 业务入口.md           # 业务问题 → 应读知识、工程入口和源码路径的导航索引
│   │   ├── architecture/         # 系统级架构图：仅存边界、组件关系、关键业务流和架构取舍
│   │   │   ├── component.puml    # 可版本管理的 PlantUML 组件图源文件
│   │   │   └── component.svg     # 组件图渲染产物，供未安装 PlantUML 的成员直接阅读
│   │   └── modules/              # 按需创建的工程模块说明，只记录源码难直接表达的高价值事实
│   │       └── <工程模块>.md     # 某复杂工程模块的跨端关系、兼容边界、生效机制和易误判点
│   ├── function/                 # 长期业务功能知识：回答“系统有什么能力、规则是什么、为何演变至今”
│   │   ├── module.json           # 业务模块轻量索引：稳定 ID、名称、别名和目录路径；不复制业务正文
│   │   └── <业务模块>/           # 一个业务领域，例如任务管理、订单管理、结算管理
│   │       ├── function.json     # 当前业务模块内的功能点轻量索引与定位映射
│   │       └── <功能点>/         # 一个最小可查询业务能力，例如任务列表、负责人筛选
│   │           ├── 功能描述.md   # 当前有效的功能、业务规则、边界和主要流程
│   │           └── 功能演变历史.md # 已验收变更、当时问题与必要取舍；解释“为什么”
│   └── workflows/                # 完整流程实例：每个目录覆盖从原始诉求到合并的全部证据链
│       ├── README.md             # 总任务导航：名称、阶段、状态、创建时间与任务目录链接
│       └── wf-<YYYYMMDD>-<shortcode>/ # 单次需求到一个 MR 的稳定实例 ID；任务完成后保留为记录
│           ├── README.md         # 本 workflow 的任务名、当前/最终状态及所有产物导航
│           ├── source-materials.md # 原始材料索引：链接、路径、来源和用途；不复制材料正文
│           ├── candidate-review.md # 候选需求、历史对齐提示、AI 扩写标记与人工审核结论
│           ├── requirement.md    # 审核发布后的正式需求单；本轮设计、开发、评审的业务基线
│           ├── design-alignment.md # 设计协作界面：方案、未决决策、风险与不确定项
│           ├── design-decision.md # 已确认设计结论：最终范围、边界、取舍和需求覆盖
│           ├── development-contract.md # 共同开发契约：所有任务共同读取的接口、数据、集成与必要共享行为
│           ├── task-package.md   # 可执行任务拆分：目标、契约引用、可改范围、验收、联调条件和硬阻塞
│           ├── development-summary.md # 实际改动、测试先行记录、Diff 概览、门禁结果和未完成项
│           ├── knowledge-update-review.md # 独立知识更新审核单：长期资产影响、更新草案、依据与人工审核结论
│           └── merge-report.md   # 代码合并依据：需求—实现—验证映射、评审、安全扫描、风险与合并建议；不承载知识回写审核
└── .codebuddy/                   # 团队共享的机器配置与运行时；除 workflows/ 外均纳入 Git
    ├── settings.json             # 内部 Plugin 市场、安装与启用声明
    ├── harness.json              # 项目适配配置：技术栈、门禁命令、超时、required 与平台集成
    ├── onboarding-checklist.json # 已确认的八项管理员接入检查，随共享配置提交 Git
    ├── rules/                    # 项目顶层架构与开发规约；按任务强制装配，不是可选风格建议
    │   ├── architecture.md       # 依赖方向、组件边界、跨模块访问与不可违反的架构约定
    │   ├── engineering.md        # 代码组织、模块职责、命名、错误处理与公共工程约定
    │   ├── testing.md            # 测试层次、命名、数据准备、验收方式与最低覆盖要求
    │   ├── api-and-data.md       # API 演进、数据模型、兼容性、迁移与持久化规约
    │   └── commit-and-mr.md      # 提交、分支、MR 描述、合并报告与审核规范
    ├── agents/                   # 独立 Agent 的职责、输入与最小权限定义
    │   └── code-reviewer.md      # 只读独立评审 Agent：读取输入包并返回结构化问题，禁止修改代码
    └── workflows/                # 机器运行态；默认 gitignore，不混入业务提交
        └── wf-<YYYYMMDD>-<shortcode>/ # 与 docs/workflows/ 同 ID 的机器侧状态与证据目录
            ├── state.json        # 流程可信进度：阶段、步骤、产物引用、确认、门禁、MR 与阻断原因
            └── evidence/         # 运行时机器证据存储；载体、数量和命名由实际命令、工具与平台决定
```

以上为业务仓库产物布局，不是插件安装包的目录树。业务模块、功能点与工程模块说明按确认后的真实名称按需创建；没有本地证据时不创建空 `evidence/`。总任务索引沿用 ADR 的导航要求，接入 checklist 对应管理员已确认的八项接入流程。初始化由单个管理员发起，主 Agent 动态分发 subagent 直接写入不重叠的文档范围；管理员完成 checklist 后提交 Git，其余成员拉取共享配置。

### 2.1 命名规则

| 区域                                 | 文件名   | 原因                      |
| ---------------------------------- | ----- | ----------------------- |
| `docs/knowledge/`、`docs/function/` | 中文    | 主要由人浏览和维护               |
| `docs/workflows/<task-id>/`        | 英文固定名 | 被状态文件、脚本、CI 和 MR 链接稳定引用 |
| `.codebuddy/`                      | 英文固定名 | 配置和运行时由机器解析             |

一个 workflow 是从原始诉求到合并的完整事务边界。候选稿、正式需求单和后续设计/开发/评审产物必须在同一任务目录；不得另建跨 workflow 的需求工作区或模板目录。第一版全部采用 Harness 内置模板。

流程产物的**概念名**与**落盘名**固定映射：

| 概念名     | 文件                           |
| ------- | ---------------------------- |
| 原始材料索引  | `source-materials.md`        |
| 候选评审稿   | `candidate-review.md`        |
| 正式需求单   | `requirement.md`             |
| 设计对齐稿   | `design-alignment.md`        |
| 设计结论    | `design-decision.md`         |
| 共同开发契约 | `development-contract.md`    |
| 任务包     | `task-package.md`            |
| 开发摘要    | `development-summary.md`     |
| 知识更新审核单 | `knowledge-update-review.md` |
| 合并报告    | `merge-report.md`            |

---

## 3. 通用节点协议

所有主链路节点都遵循同一套节点协议。具体模板可以不同，但必须能回答同样五个问题。

```text
输入事实：   本节点必须读取的文件、代码、配置与外部快照
输出产物：   本节点写入的文件与固定字段
准入门槛：   什么条件满足才允许开始
完成证据：   什么事实证明本节点已完成
回退规则：   哪些情况必须退回上游，退到哪一步
```

### 3.1 机器侧交接对象

`state.json` 不复制 Markdown 正文，只保存路径、状态和可验证结果。所有路径相对项目根目录。

```json
{
  "schema_version": "<schema-version>",
  "task_id": "<workflow-id>",
  "title": "<task-title>",
  "stage": "<current-stage>",
  "step": "<current-step>",
  "status": "<current-status>",
  "summary": "<human-readable-current-summary>",
  "next_action": "<next-legal-action>",
  "artifacts": {
    "<artifact-key>": "<workflow-artifact-path>",
    "evidence_refs": "<runtime-generated-evidence-reference-array>"
  },
  "approvals": {
    "<approval-key>": {
      "status": "<pending-or-approved-or-rejected>",
      "by": "<approver-when-applicable>",
      "at": "<timestamp-when-applicable>"
    }
  },
  "gates": "<runtime-generated-gate-results>",
  "mr": "<mr-state-when-applicable>",
  "blockers": "<runtime-generated-blocker-array>",
  "created_at": "<timestamp>",
  "updated_at": "<timestamp>"
}
```

### 3.2 交接统一规则

1. **下游只消费已落盘产物**：不得要求下一节点读取上一 Agent 的聊天记录。
2. **状态写入由统一脚本完成**：节点或 SubAgent 只返回结果，不能随意改 `state.json`。
3. **上游产物缺失即不允许进入下游**：不能用自然语言“我已经看过了”补齐。
4. **人工确认是状态事实**：确认结果必须写入 `approvals`，不能只出现在聊天里。
5. **回退不覆盖历史**：回退后重写目标产物，并更新状态；旧版本由 Git 保存。
6. **证据引用不预设文件形态或数量**：人读模板只引用逻辑证据 ID、产出方、适用对象与生成时的 Commit；不得在模板或示例中写死某个日志文件名、扩展名、目录层级或“必须一份/必须多份”的证据形态。运行时按实际命令、工具和平台产出零至多份机器证据，由状态文件的 `artifacts.evidence_refs` 统一索引。
7. **模板只定义字段契约，不提供业务实例**：模板内动态值一律写为 `<...>` 占位符；不得把某个任务标题、需求 ID、代码路径、接口参数、命令、测试名、断言数量、平台名称、状态结论或“无阻断”等局部事实作为 few-shot 示例。固定的只有流程文件名、字段名、枚举与准入规则。完整场景只能放在独立的“全流程模拟”章节，并显式标注为非模板、禁止作为默认值加载。

### 3.3 证据引用契约

证据不是 Markdown 中猜测出来的文件路径，而是绑定一次执行快照的逻辑引用。每条引用至少包含：

```json
{
  "id": "EV-<runtime-generated-id>",
  "kind": "gate_result | review_result | security_result | diff | test_result | external_link",
  "producer": "产生该证据的命令、工具或平台",
  "subject": "证明的对象或验收项",
  "commit": "证据对应的 MR Commit",
  "location": "运行时生成的可访问位置"
}
```

`location` 由运行时填写，可以是一个文件、多个文件的索引、平台链接或内联结构化结果；模板不得假定其数量、名称或后缀。人读报告只写 `EV-...` 及其证明含义；需要排障时再通过状态文件解析真实位置。

---

## 4. 模块一：需求整理与发布

### 4.1 节点职责

将原始材料整理为候选需求，经过人工业务裁定后，发布为本轮唯一正式需求单。

### 4.2 输入

| 类型     | 必须性 | 内容                                             |
| ------ | --- | ---------------------------------------------- |
| 原始材料   | 必须  | TAPD、PRD、会议纪要、聊天记录、客户邮件、截图或口述转写                |
| 功能描述   | 按需  | 已定位到对应功能点时读取                                   |
| 功能演变历史 | 条件  | 出现重复、冲突、兼容性问题时读取                               |
| 顶层规则   | 必须  | `architecture.md`、`engineering.md` 中与需求范围有关的边界 |

### 4.3 原始材料索引模板

**路径**：`docs/workflows/<task-id>/source-materials.md`

该文件只做定位，不复制原始材料正文，避免同一事实在 workflow 内形成第二份可漂移副本。创建 workflow 时必须全量扫描项目可见文件并先写入该索引；不得预设 README、`docs/`、特定目录名或文件扩展名才可能是原始材料。只可排除 Git、依赖目录和 Harness 自身的 workflow 运行态；用户当轮原话、链接与外部材料另以定位项追加。

```md
# 原始材料索引

- 任务 ID：<workflow-id>
- 创建时间：<timestamp>

| 编号 | 来源类型 | 位置 | 提供人/系统 | 使用说明 |
| --- | --- | --- | --- | --- |
| <source-id> | <source-type> | <runtime-resolved-location> | <provider> | <why-this-source-is-read> |
```

### 4.4 候选评审稿模板

**路径**：`docs/workflows/<task-id>/candidate-review.md`

原始材料不复制进候选稿；来源链接、文件路径、提出人和材料说明统一放在同目录 `source-materials.md`。

```md
# 候选需求评审稿

## 基本信息
- 任务 ID：<workflow-id>
- 来源索引：`source-materials.md`
- 整理时间：<timestamp>
- 整理范围：<in-scope-business-area>

## 历史对齐
| 候选项 | 关联功能 | 判断 | 依据 |
| --- | --- | --- | --- |
| <candidate-id> | <related-function> | <可能已存在/可能冲突/可能复用/无可用历史> | <fact-based-rationale> |

## 候选需求

### <candidate-id> · <candidate-title>
- 类型：<用户故事/业务规则/质量或技术约束>
- 主张：<candidate-statement>
- 业务级验收标准：
  1. <observable-acceptance-criterion>；
- AI 扩写：<是/否>
- 历史提示：<historical-alignment-result-or-none>
- 审核结论：<待审核/通过/需修改/合并/拆分/驳回/暂缓>
- 审核意见：<human-review-comment>
```

### 4.5 正式需求单模板

**路径**：`docs/workflows/<task-id>/requirement.md`

正式需求单只保留可进入设计的事实，不携带来源证据、审核过程或 `【AI扩写】` 标记。

```md
# 正式需求单

## 基本信息
- 任务 ID：<workflow-id>
- 标题：<task-title>
- 发布状态：<published-status>
- 发布人：<publisher>
- 发布时间：<timestamp>

## 需求项

### <requirement-id> · <requirement-title>
- 类型：<用户故事/业务规则/质量或技术约束>
- 需求主张：<approved-requirement-statement>
- 业务级验收标准：
  1. <observable-acceptance-criterion>；
```

### 4.6 放行与回退

| 项目     | 规则                                                                      |
| ------ | ----------------------------------------------------------------------- |
| 设计节点准入 | `requirement.md` 存在；至少一项需求；每项均有业务级验收标准；`requirement_published=approved` |
| 回退到需求  | 角色、业务价值、范围或验收标准不清；设计发现业务规则冲突且无法从事实判断                                    |
| 不允许做   | 在本节点写接口字段、数据库表结构、具体实现文件或任务拆分                                            |

---

## 5. 模块二：工程探索、设计对齐与任务拆分

### 5.1 设计节点输入装配

设计节点必须先装配以下上下文，而不是从需求直接写代码：

| 优先级 | 必读内容                                 | 用途                |
| --- | ------------------------------------ | ----------------- |
| P0  | `requirement.md`                     | 本轮范围与验收标准         |
| P0  | 匹配的 `.codebuddy/rules/`              | 架构边界、测试方式、接口与数据规约 |
| P1  | `docs/knowledge/业务入口.md`             | 定位工程入口和应读文件       |
| P1  | 目标源码、接口定义、配置                         | 实现事实核验            |
| P2  | `docs/function/<业务模块>/<功能点>/功能描述.md` | 当前业务规则            |
| P3  | 功能演变历史、模块工程说明、UML                    | 仅在兼容性、跨模块、架构变更时读取 |

### 5.2 设计对齐稿模板

**路径**：`docs/workflows/<task-id>/design-alignment.md`

```md
# 设计对齐稿

## 1. 需求理解
- 本轮改变：<intended-change>。
- 本轮不改变：<explicitly-out-of-scope-items>。
- 验收标准对应：
  | 需求验收标准 | 设计响应 |
  | --- | --- |
  | <requirement-criterion> | <design-response> |

## 2. 改动范围
- 业务模块：<business-module / function>
- 读取的工程入口：
  - `<runtime-resolved-code-or-config-location>`
- 预计修改：
  - <component-or-module>：<expected-change>。
- 必须保持：
  - <existing-contract-or-boundary>。

## 3. 架构变化
- 结论：<architecture-impact-or-not-applicable>。
- UML：<updated-diagram-reference-or-not-applicable>。

## 4. 待人确认的决策

### <decision-id> · <decision-title>
- 推荐方案：<recommended-option>。
- 可选方案：<alternative-options-or-none>。
- 影响：<business-technical-cost-or-compatibility-impact>。
- 需要确认：<decision-question>。
- 状态：<pending/confirmed/rejected>。

## 5. 风险与不确定项
- <risk-id>：<unknown-fact-or-risk-and-verification-plan>。
```

### 5.3 设计结论模板

**路径**：`docs/workflows/<task-id>/design-decision.md`

设计结论只保留已经确认、可执行的内容；候选方案和争论过程仍留在设计对齐稿。

```md
# 设计结论

## 已确认范围
- <approved-change-scope>。

## 实现边界
- <layer-or-component>：`<runtime-resolved-location>`

## 需求覆盖
| 需求项 | 实现策略 | 验证方式 |
| --- | --- | --- |
| <requirement-or-criterion-id> | <implementation-strategy> | <verification-method> |

## 已确认取舍
- <confirmed-trade-off-and-rationale>。
```

### 5.4 共同开发契约模板

**路径**：`docs/workflows/<task-id>/development-contract.md`

该文件是所有开发任务共同读取的唯一契约正文。只装配本轮适用的 HTTP API、公共接口、数据、跨任务集成和共享行为块；普通状态、权限和行为结果留在验收标准，只有多个任务必须共享同一语义时才提升为共享行为契约。不适用的块不生成。

```md
# 共同开发契约

## 契约范围
- 适用需求：<requirement-or-criterion-id-list>。
- 适用任务：<task-id-list>。
- 明确排除：<explicitly-excluded-contract-scope>。

## 契约清单
| 契约 ID | 类型 | 提供方 | 使用方 | 实现事实源 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| <contract-id> | <API/公共接口/数据/共享行为/跨任务集成> | <provider-task-or-component> | <consumer-task-or-component> | `<runtime-resolved-source-location>` | <verification-method> |

<applicable-contract-blocks>

## 全任务共同门禁
| 门禁 | 适用任务 | 通过条件 |
| --- | --- | --- |
| <runtime-discovered-gate-name> | <task-id-list> | <observable-pass-condition> |
```

按需契约块的固定结构如下；插件内逐项模板位于 `templates/contracts/`，主文档只插入本轮实际涉及的块。

| 类型 | 固定标题与表头 |
| --- | --- |
| HTTP API | `端点`（方法、路径、用途、提供任务、使用任务）；`请求`（字段、类型、必填、约束）；`响应`（字段、类型、可空、语义）；`错误语义`（HTTP 状态、错误码、触发条件、调用方行为）；`兼容要求` |
| 公共接口 | `接口定义`（接口或命令、签名或用法、提供任务、使用任务）；`参数`（参数、类型、必填、约束）；`返回或输出`（内容、类型、语义）；`异常或退出码`（触发条件、异常或退出码、调用方行为） |
| 数据 | `数据结构`（结构、用途、生产方、消费方）；`字段`（字段、类型、结构或取值、必填/可空、约束、本轮变化）；`一致性与兼容` |
| 跨任务集成 | `任务接口`（提供任务、使用任务、输入、输出、错误语义）；`Mock / Stub`（使用任务、替代对象、模拟内容、必须保持一致、结束条件）；`联调时机`（参与任务、准入条件、联调动作、通过条件） |
| 共享行为 | 单表：共同语义、适用任务、对应验收标准、验证方式；仅用于多个任务共同依赖的状态、权限、事务或并发语义 |

### 5.5 任务包模板

**路径**：`docs/workflows/<task-id>/task-package.md`

```md
# 任务包

## 共同开发契约
- [共同开发契约](development-contract.md) 是所有任务必须读取的唯一契约正文。

## <task-id> · <task-title>
- 任务目标：<independently-deliverable-engineering-result>。
- 负责契约：<provided-contract-id-list>。
- 使用契约：<consumed-contract-id-list>。
- 可改范围：
  - `<runtime-resolved-file-or-module-boundary>`
- 必须读取：
  - `requirement.md`
  - `design-decision.md`
  - `development-contract.md`
  - `<matched-rule-path>`
  - `<necessary-code-or-knowledge-location>`
- 验收标准：
  1. <observable-task-acceptance-criterion>；
- 联调条件：<integration-input-output-and-timing-or-none>。
- 硬阻塞：<blocking-prerequisite-or-none>。
```

### 5.6 设计节点放行与回退

| 项目      | 规则                                                             |
| ------- | -------------------------------------------------------------- |
| 开发节点准入  | `design-decision.md`、`development-contract.md` 与 `task-package.md` 完成；无待确认关键决策；契约清单与按需块一致；任务契约 ID 均存在；三份产物已提交且工作区干净；任务包人工确认开工 |
| 回退到设计对齐 | 共同开发契约未定或需修改、任务验收不可独立、任务范围冲突或存在真实硬阻塞                                 |
| 回退到需求   | 设计发现业务目标、边界或验收标准本身需要重新裁定                                       |

---

## 6. 模块三：开发、测试与门禁

### 6.1 开发节点输入

| 内容                   | 是否必须 | 目的                 |
| -------------------- | ---- | ------------------ |
| `task-package.md`    | 必须   | 当前任务、可改范围和验收标准     |
| `design-decision.md` | 必须   | 已确认范围、边界、覆盖与取舍     |
| `development-contract.md` | 必须 | 所有开发任务共同遵守的唯一契约正文 |
| 匹配的顶层规则              | 必须   | 架构、工程、测试、数据或 MR 规约 |
| 目标源码与测试              | 必须   | 真实实现和已有模式          |
| `requirement.md`     | 必须   | 防止实现偏离业务验收         |


### 6.2 开发摘要模板

**路径**：`docs/workflows/<task-id>/development-summary.md`

```md
# 开发摘要

## 本轮完成情况
| 任务 | 状态 | 实现位置 | 验收结果 |
| --- | --- | --- | --- |
| <task-id> | <completed/partial/blocked> | `<runtime-resolved-implementation-location>` | <criterion-coverage-and-result-summary> |

## 测试先行记录
| 任务 | 先新增/更新的测试 | 初始状态 | 最终状态 | 验证证据 |
| --- | --- | --- | --- | --- |
| <task-id> | `<runtime-resolved-test-location-or-identifier>` | <initial-observation> | <final-observation> | `EV-<runtime-generated-id>`：<what-the-evidence-proves> |

## Diff 概览
- <change-kind>：`<runtime-resolved-location>`；<change-purpose>。

## 门禁结果
| 检查 | 实际执行 | 结果 | 证据 |
| --- | --- | --- | --- |
| <runtime-discovered-gate-name> | `<actual-command-or-platform-action>` | <passed/failed/not_configured/not_run> | `EV-<runtime-generated-id>`：<what-the-evidence-proves> |

## 未完成项与阻断
- <none-or-unresolved-item-with-impact-and-next-action>。

## 提交 MR 前请人复核
- <whether-each-business-acceptance-criterion-has-implementation-and-verification>；
- <whether-any-not-configured-or-failed-gate-is-acceptable>；
- <whether-diff-stays-within-approved-scope>。
```

### 6.3 开发节点规则

1. 每个任务先用验收标准表达失败测试，再做最小实现；
2. 允许按任务并行，但集成点必须汇总运行整轮门禁；
3. 门禁未通过时，开发节点保持 `blocked` 或 `running`，不得写“可验收”；
4. “未配置”是事实状态，不等于通过；
5. 人只在整轮摘要出来后确认是否提交 MR，不逐任务接管。

---

## 7. 模块四：独立评审、修复与合并报告

### 7.1 评审输入包

独立 `code-reviewer` 只接收以下五项，不接收开发 Agent 的对话和自述。

```json
{
  "mr": {
    "id": "<mr-identifier>",
    "head_commit": "<reviewed-commit>"
  },
  "evidence_refs": "<runtime-generated-evidence-reference-array-including-diff>",
  "artifacts": {
    "requirement": "<requirement-artifact-path>",
    "design_decision": "<design-decision-artifact-path>",
    "task_package": "<task-package-artifact-path>",
    "development_summary": "<development-summary-artifact-path>"
  },
  "rules": "<task-matched-rule-path-array>"
}
```

### 7.2 结构化评审结果

结构化评审结果作为 `kind=review_result` 的运行时证据写入 `artifacts.evidence_refs`；实际承载位置由运行时决定，不在模板中规定文件名、文件数量或扩展名。

```json
{
  "schema_version": "<schema-version>",
  "reviewed_commit": "<reviewed-commit>",
  "conclusion": "<pass/fail/needs_human_judgment>",
  "findings": "<runtime-generated-finding-array; may be empty>",
  "coverage": "<runtime-generated-requirement-coverage-array>"
}
```


### 7.3 知识更新审核单模板

**路径**：`docs/workflows/<task-id>/knowledge-update-review.md`

该文件是长期知识与顶层 Rules 的**独立审核交付物**，不并入 `merge-report.md`，也不允许以“无变化”一句话跳过。它在独立代码评审通过后、最终 MR 批准前生成；长期资产只有在该文件经人工确认后，才能按审核结论写入同一 MR。

```md
# 长期知识更新审核单

## 1. 审核对象
- 任务 ID：<workflow-id>
- MR / Commit：<mr-identifier> / <reviewed-commit>
- 需求与设计依据：`requirement.md`、`design-decision.md`
- 代码与验证依据：`development-summary.md`、`EV-<runtime-generated-id>`（<evidence-purpose>）
- 审核状态：<pending/approved/returned/rejected>

## 2. 影响判定总览
| 长期资产 | 判定 | 理由 | 本轮动作 | 证据 |
| --- | --- | --- | --- | --- |
| `<required-long-term-asset>` | <需要更新/无需更新/待人裁定> | <fact-based-rationale> | <update/no-change/human-decision> | `EV-<runtime-generated-id>`：<what-the-evidence-proves> |

## 3. 更新草案
### <update-id> · <update-title>
- 目标文件：`<approved-long-term-asset-path>`
- 草案：<proposed-current-fact-or-rule-change>。
- 替代/删除内容：<superseded-content-or-none>。
- 保留理由：<why-this-knowledge-must-remain-long-lived>。

## 4. 审核结论
- 审核人：<reviewer>
- 审核时间：<timestamp>
- 结论：<通过/退回修改/不通过>
- 审核意见：<review-comment>
- 已批准更新项：<approved-update-id-array>

## 5. 实施记录
- 实施状态：<待实施/已实施/实施失败>
- 实际修改文件：<runtime-resolved-changed-location-array>
- 知识更新 Commit：<knowledge-update-commit>
- 最终独立评审 Commit：<final-reviewed-commit>
- 说明：知识更新产生新 Commit 后，必须重新执行独立评审；未通过前不得生成最终合并报告。
```

**判定枚举**只允许：`需要更新`、`无需更新`、`待人裁定`。每个长期资产都必须逐项出现；“无需更新”必须写理由和依据，不能省略。`待人裁定` 未清零时，不得进入 MR 最终审核。

执行规则：AI 先提交审核单和更新草案，不直接改写长期资产；人审核通过后，AI 才在同一分支实际更新对应文件、刷新自动生成的文件树，并将实际变更路径与 Commit 写回该审核单。Rules 只有在沉淀出跨任务、长期稳定且必须遵守的规约时才允许更新，不能把本轮局部实现细节塞入 Rules。


### 7.4 合并报告模板

**路径**：`docs/workflows/<task-id>/merge-report.md`

```md
# 代码合并报告

## 1. 基本信息
- MR：<mr-identifier>
- 分支：<source-branch>
- Commit：<final-reviewed-commit>
- 任务 ID：<workflow-id>
- 生成时间：<timestamp>

## 2. 本次变更
- 目标：<approved-change-goal>。
- 影响范围：<actual-changed-components-or-areas>。
- 不涉及：<explicitly-out-of-scope-items>。

## 3. 需求实现与验证
| 需求/验收标准 | 关联任务 | 实现位置 | 验证证据 | 覆盖状态 |
| --- | --- | --- | --- | --- |
| <requirement-or-criterion-id-and-summary> | <task-id> | `<runtime-resolved-location>` | `EV-<runtime-generated-id>`：<what-the-evidence-proves> | <covered/partially-covered/not-covered/not-applicable> |

## 4. 测试与质量门禁
| 检查 | 结果 | 证据 |
| --- | --- | --- |
| <runtime-discovered-gate-name> | <passed/failed/not_configured/not_run> | `EV-<runtime-generated-id>`：<what-the-evidence-proves> |

## 5. 独立代码评审
- BLOCKER：<runtime-generated-count>。
- WARNING：<runtime-generated-count-and-status>。
- INFO：<runtime-generated-count-and-status>。

## 6. 安全扫描
- 状态：<passed/failed/not_configured/not_run>。
- 说明：<scan-scope-findings-and-any-exemption>。

## 7. 遗留问题与风险
- <none-or-unresolved-item-with-impact-and-recommendation>。

## 8. 合并结论
- 建议：<可以合并/不建议合并/需要人工判断>。
- 前提：<remaining-platform-or-approval-condition>。
```

### 7.5 评审、知识审核与修复闭环

| 结果             | 下一动作                                                            | 状态变化                       |
| -------------- | --------------------------------------------------------------- | -------------------------- |
| 无 BLOCKER      | 生成知识更新审核单；先等待知识审核，不生成最终合并报告                                     | `review` → `waiting_human` |
| 知识审核退回或存在待人裁定  | 修改审核单或更新草案，重新提交知识审核                                             | `review` → `waiting_human` |
| 知识审核通过         | 在同一分支实施已批准的知识更新，刷新审核单的实际变更记录；因 MR 出现新 Commit，重新执行独立评审后才生成最终合并报告 | `review` → `running`       |
| 有 BLOCKER，未超两轮 | 回开发修复、重跑整轮门禁、更新 MR、全新上下文复审                                      | `review` → `development`   |
| 两轮后仍有 BLOCKER  | 停止自动循环，交人处理                                                     | `review` → `blocked`       |
| 新 Commit 出现    | 当前评审报告、知识审核单与批准失效，重新评审并重新做影响判定                                  | `review` → `running`       |

---

## 8. 项目适配：只配置事实与策略

### 8.1 通用能力与项目差异

| 类别   | 内容                             | 放置位置                               | 谁维护                |
| ---- | ------------------------------ | ---------------------------------- | ------------------ |
| 通用流程 | 节点模板、交接校验、状态脚本、报告结构            | Harness 运行时                        | Plugin / Skill 发布方 |
| 项目事实 | 技术栈、入口、架构事实、功能事实               | `docs/knowledge/`、`docs/function/` | 初始化器生成草案，人确认       |
| 顶层规约 | 架构边界、工程约定、测试策略、数据/API 原则、MR 规范 | `.codebuddy/rules/`                | 项目负责人确认和维护         |
| 项目策略 | 可执行门禁、超时、required、CI 适配器       | `.codebuddy/harness.json`          | 初始化器生成草案，人确认       |


### 8.2 `harness.json` 最小配置格式

用户不填写流程步骤；只确认初始化器已识别出的项目事实与策略。

```json
{
  "schemaVersion": "<schema-version>",
  "adapterVersion": "<adapter-version>",
  "project": {
    "stack": "<runtime-discovered-stack-array>",
    "packageManager": "<runtime-discovered-package-manager-or-none>",
    "sourceRoots": "<runtime-discovered-source-root-array>",
    "testRoots": "<runtime-discovered-test-root-array>"
  },
  "protectedContracts": "<project-required-contract-path-array>",
  "gates": {
    "preCommit": "<runtime-discovered-gate-definition-array>",
    "ci": "<runtime-discovered-gate-definition-array>"
  },
  "integrations": {
    "workItem": "<configured-platform-or-none>",
    "codeReview": "<configured-platform-or-none>",
    "ci": "<configured-platform-or-none>"
  }
}
```

### 8.3 初始化器交互

初始化器必须先探索，再让负责人确认。对外呈现为“已识别结果 + 少量待确认项”，不是大表单。

```text
已识别：<技术栈>；源码目录 <source-root-array>；测试目录 <test-root-array>。
已识别门禁：<runtime-discovered-gate-array>。
未识别：<missing-project-capability-array>。
建议：将已验证的项目事实写入 harness.json；未识别项明确标记未配置，不得猜测或补造。
请确认：接受 / 编辑某项 / 暂不接入。
```

初始化链路由实际文件树动态决定：先全量扫描每个可见项目文件和目录并生成路径骨架，不预设 README、`docs/`、目录名或文件扩展名是项目事实位置；侦察 subagent 以骨架建立文件所有权和依赖，工程/业务 subagent 分区读取各自路径并直接回写用途和文档，汇总 subagent 等所有可读路径完成后再写项目总览、业务入口和架构图，Rules subagent 等知识入口落盘后写五份 Rules。主 Agent 只负责调度、失败重派和确定性结构校验，不增加中间人工审核回合。顶层 Skill 用自然语言逐项询问八项 checklist；负责人确认每项后由 Skill 写入 `.codebuddy/onboarding-checklist.json`，不要求人编辑 JSON。只有草案字段齐全、无占位符、每个文件树路径都有具体说明且八项确认完成，`harness init --apply --knowledge <draft.json>` 才写入全部非-workflow 的规范长期资产：`docs/knowledge/`、`docs/function/`、`.codebuddy/rules/`、组件图源文件及默认供人阅读的 SVG。prepare、subagent 回写和 finalize 必须属于同一顶层编排会话；脱离该会话的 CLI finalize 对已有长期资产按冲突处理，不用未持久化清单覆盖。无知识草案不得应用，更不得生成看似完成的占位骨架。不得生成未登记的顶层知识文件（包括 `gates.md`、`harness-self.md`、`核心约束.md`、`启动入口.md`）。

### 8.3.1 自然语言启动与需求阶段止损

用户以自然语言要求“开始一轮开发”时，运行时只创建该 workflow 的十一份规范产物与 `state.json`，其中 `source-materials.md` 已由全量项目扫描填入可见文件索引；初始状态必须是 `stage=requirement`、`step=source_materials`、`next_action=record_source_materials`。本次调用只追加用户原话、链接或外部文件位置，再停止；不得因为描述模糊或看似完整，就自动生成候选结论、正式需求、设计、共同开发契约、任务包或开发内容。

后续调用严格一节点一停：`source_materials → candidate_review → publish_requirement → design`。候选评审只能在下一次明确请求后写入；正式 `requirement.md` 只能在负责人明确批准候选后写入；批准写入后状态的下一动作是 `advance_to_design`，仍须等下一次用户调用。每次自然语言调用只可执行当前 `next_action` 对应的一次状态转换；任何跳步都由状态机拒绝。

### 8.4 Rules 的任务装配映射

`CODEBUDDY.md` 常驻保存索引与触发条件。进入节点后，主流程按任务类型读取相关规则。

| 操作              | 必读规则                                          |
| --------------- | --------------------------------------------- |
| 新建或重构模块         | `architecture.md`、`engineering.md`            |
| 修改 API、数据模型、持久化 | `architecture.md`、`api-and-data.md`           |
| 新增或修改测试         | `testing.md`                                  |
| 提交 MR、生成报告      | `commit-and-mr.md`                            |

### 8.5 0.8.0 Markdown 结构合同与动态初始化

0.8.0 起，以下结构是生成项目的唯一 Markdown 合同，模板和 `doctor` 均通过显式 allowlist 解析标题、章节顺序、表头和允许的 `<...>` 占位符：

| 文件 | 固定结构 |
| --- | --- |
| `CODEBUDDY.md` | `项目导航`、`阅读顺序与上下文边界`、`Rules 装配索引`、`全局行为规则` |
| `docs/knowledge/项目总览.md` | `项目用途`、`技术栈`、`软件设计架构`、`启动、构建与测试入口`、`顶层模块`、`未识别项`、`事实依据`；软件设计架构紧跟技术栈 |
| `docs/knowledge/文件树.md` | 探索态为路径骨架；完成态每个可见文件和目录均有基于阅读的一句话用途，不得出现“源码目录”“业务逻辑”“用途待确认”等泛化词 |
| `docs/knowledge/业务入口.md` | 五列表头：业务问题/场景、业务模块/功能点、优先阅读的业务知识、工程知识入口、源码/配置入口 |
| `docs/knowledge/modules/<工程模块>.md` | 模块定位、目录与入口、核心组成、主要流程、跨端关系、兼容边界、生效机制、易误判点、事实依据 |
| `docs/function/<业务模块>/<功能点>/功能描述.md` | 所属业务模块、当前状态，以及当前功能、业务规则、边界、主要流程、事实依据 |
| `.codebuddy/rules/*.md` | 首行 `适用场景:`，随后必须遵守、相关知识入口、验证方式、更新门槛；Rules 只做约束和索引，不复制知识正文 |
| `docs/workflows/<task-id>/development-contract.md` | 契约范围、契约清单、一个或多个按需类型块、全任务共同门禁；清单 ID/类型必须与类型块一一对应 |

初始化由主 Agent 按全量文件树动态构建 subagent 依赖图。侦察任务先写路径骨架并为每个可读路径分配唯一阅读负责人；工程/业务负责人只能写各自不重叠的文档与树项，汇总任务等待所有阅读结果，Rules 任务等待知识入口。负责人不审核 subagent 的中间稿，主 Agent 只在最终结构检查失败时重派相应负责人。Git 是共享版本和历史依据；不引入 digest、内容哈希、CAS 或额外初始化锁。未接入 Harness 的项目不触发 Hook，只有存在有效 `.codebuddy/harness.json` 时才执行 Harness 门禁与保护。
| 只读探索陌生代码        | `architecture.md` + 对应 `docs/knowledge/` 导航文档 |

规则是顶层约束，**按任务强制装配**；不是等模型“想起来”再读取。能用脚本明确检测的规则，再额外配置到 Hook 或 CI 门禁；不能明确检测的规则由设计对齐、测试和独立评审共同校验。

---

## 9. 全流程模拟：任务列表新增负责人筛选

> **非模板，禁止复制为运行时默认值。** 本节仅用于检验前文抽象字段能否串成一次完整事务；其中的任务名称、路径、命令、测试、验收数量、平台状态与结论均为该模拟场景的局部事实，不得被模板、Rules、Skill 或运行时当作通用规范加载。实际 workflow 只从第 3–8 节的字段契约与项目探索结果生成。

本节不描述抽象流程，而是按真实文件、字段和状态推进一次完整开发。

### 9.1 场景与初始状态

- 项目：一个 PHP + Vue 的项目管理系统；
- 当前已有：任务列表、状态筛选、分页、任务负责人字段；
- 原始诉求：项目经理希望“在任务列表按负责人看待办”；
- 未接外部需求系统，需求与流程产物均落盘；
- 项目已有 `make phpstan`、`make test-code-style`、`make unit-test`；未配置安全扫描。

初始状态：

```json
{
  "task_id": "wf-20260907-taskfilter",
  "stage": "requirement",
  "step": "candidate_review",
  "status": "waiting_human",
  "artifacts": {
    "source_materials": "docs/workflows/wf-20260907-taskfilter/source-materials.md",
    "candidate_review": "docs/workflows/wf-20260907-taskfilter/candidate-review.md"
  },
  "approvals": {
    "requirement_published": { "status": "pending" }
  }
}
```

### 9.2 需求节点

1. Agent 建立 `source-materials.md`，登记会议纪要与既有需求链接；
2. Agent 读取会议纪要，按 `docs/function/任务管理/任务列表/功能描述.md` 定位到现有筛选能力；
3. 发现状态筛选存在 URL 同步，写入 `candidate-review.md` 的“可能复用”提示；
4. 负责人确认 R-01，通过并发布；
5. Agent 生成 `requirement.md`；状态更新为设计阶段。

状态变化：

```text
requirement/candidate_review/waiting_human
→ requirement/published/completed
→ design/explore/running
```

### 9.3 设计节点

1. Agent 读取 `requirement.md`；
2. 根据 `docs/knowledge/业务入口.md` 定位 `TaskQueryService` 与 `TaskList.vue`；
3. 读取 `architecture.md`、`engineering.md`、`testing.md`；
4. 核验任务是否多负责人；发现模型支持负责人列表；
5. 写 `design-alignment.md`，将“多负责人命中语义”作为 D-01；
6. 负责人选择“只要包含所选成员即命中”；
7. Agent 依次写 `design-decision.md`、`development-contract.md`、`task-package.md`；逐任务核对负责/使用的契约 ID 后，将三份产物提交 Git，负责人基于该 Git 版本确认开工。确认后任一产物变化，必须回到设计并重新确认。

状态变化：

```text
design/explore/running
→ design/alignment/waiting_human
→ design/task_package/waiting_human
→ development/implement/running
```

### 9.4 开发节点

1. 对 T-01，先在 `TaskListFilterTest.php` 写负责人过滤失败测试；
2. 在查询服务最小实现 `assignee_id` 过滤；测试转绿；
3. 对 T-02，先写列表 URL 参数同步测试；
4. 增加筛选控件，复用既有状态筛选工具；
5. 集成后依次执行门禁；构建门禁未配置，明确记录“未配置”；
6. 生成 `development-summary.md`；负责人复核后允许创建 MR。

`gates` 字段示例：

```json
{
  "phpstan": { "status": "passed", "exitCode": 0 },
  "style": { "status": "passed", "exitCode": 0 },
  "unit-tests": { "status": "passed", "exitCode": 0 },
  "build": { "status": "not_configured" }
}
```

### 9.5 评审节点

1. 主流程将 MR Diff、`requirement.md`、`design-decision.md`、`development-contract.md`、`task-package.md`、`development-summary.md` 和相关 rules 组装给只读 reviewer；
2. reviewer 验证：查询仍走现有权限作用域；URL 参数未破坏状态与分页；R-01 三项验收均有测试；
3. reviewer 返回 `BLOCKER=0`、`WARNING=0`、`INFO=1`；
4. 安全扫描记录为 `not_configured`，不伪造通过；
5. 生成独立的 `knowledge-update-review.md`，逐项审核项目总览、文件树、业务入口、架构、工程模块、功能知识和 Rules 的影响；
6. 负责人审核知识更新草案，确认仅更新文件树与任务列表的两份业务知识；Agent 在同一分支完成实际回写，并把实际修改路径写回审核单；
7. 知识回写产生新 Commit，因此重新执行独立评审，确保最终 MR 快照仍无 BLOCKER；
8. 基于最终 Commit 生成 `merge-report.md`；
9. 平台审核人依据 MR、合并报告和已批准的知识更新审核单批准；合并后状态标记 completed。

最终状态：

```json
{
  "stage": "completed",
  "step": "merged",
  "status": "completed",
  "approvals": {
    "requirement_published": { "status": "approved" },
    "task_package": { "status": "approved" },
    "development_summary": { "status": "approved" },
    "knowledge_update_review": { "status": "approved" },
    "mr": { "status": "approved" }
  },
  "mr": {
    "id": "!128",
    "head_commit": "ab12cd34",
    "merged_commit": "f98e7d65"
  }
}
```

### 9.6 合并前的知识更新审核与回写

独立代码评审通过后、MR 最终批准前，Agent 先生成 `knowledge-update-review.md`。负责人审核草案并确认后，Agent 才在同一分支更新长期资产：

```text
docs/function/任务管理/任务列表/功能描述.md
  新增：负责人筛选的当前行为、无负责人任务处理、URL 参数说明

docs/function/任务管理/任务列表/功能演变历史.md
  新增：2026-09-07 新增负责人筛选；取舍为复用既有状态筛选 URL 同步机制
```

`docs/workflows/wf-20260907-taskfilter/` 保留为历史任务记录，但不再作为后续任务的默认上下文；后续需求优先从 `docs/function/` 读取当前事实。

---

## 10. 最小验收清单

以下条件同时满足，才说明“模块模板与交接协议”已能工作：

| 编号 | 验收项                  | 可观察证据                                                                                 |
| -- | -------------------- | ------------------------------------------------------------------------------------- |
| A1 | 每个主节点有固定 Markdown 模板 | 全部 workflow Markdown 产物的固定章节完整，且动态值均由字段契约生成 |
| A2 | 下游不依赖上游对话            | 恢复会话时仅凭 `state.json` 与 artifacts 可继续                                                  |
| A3 | 四个人工闸口是机器可见事实        | `approvals` 有 requirement、task package、development summary、knowledge update review 四项 |
| A4 | 设计不能直跳开发             | 无 `task_package=approved` 时状态脚本拒绝进入 development                                       |
| A5 | 评审独立                 | reviewer 输入包无开发对话，且无写权限                                                               |
| A6 | 未配置能力不伪造通过           | `development-summary.md` 与 `merge-report.md` 显示 `not_configured`                      |
| A7 | 项目适配不靠大表单            | 初始化器输出识别草案，负责人仅确认差异、风险与命令                                                             |
| A8 | Rules 被按任务装配         | 设计、开发和评审输入清单均包含匹配 rules 路径                                                            |
| A9 | 知识回写独立审核             | `knowledge-update-review.md` 逐项覆盖长期资产、含依据与审核结论；无批准不得合并                                |

---

## 11. 后续实现优先级

1. 先实现流程模板生成和 `state.json` 产物引用；
2. 再实现状态转换校验与三个人工确认写入；
3. 再实现规则按任务类型的装配表；
4. 最后接入项目门禁执行器和独立 reviewer。

不先做自动知识初始化、向量检索、规则自进化或多任务并发。先拿一个真实小需求跑通上述模拟链路，再根据失败点补机制。
